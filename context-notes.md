<!-- Tistory 원문 이관에 필요한 조사 결과와 구현 제약 -->
# 조사 메모

## 2026년 8월 Notion 이관 메모

- 범위는 서울 시간 2026-08-01 00:00부터 2026-08-09 현재까지다.
- Notion 검색은 연결 사용자 `GILWON YOO`가 직접 생성한 항목으로 제한했다.
- 검색 API는 날짜만으로 전체 목록을 반환하지 않아 여러 넓은 검색어의 합집합을 사용한다.
- `노시언의 30가지 노션템플릿`은 같은 시각에 다수 하위 페이지·데이터베이스를 만들었다. 최초 이관은 상위 콘텐츠로 제한했지만, 추가 요구에 따라 하위 원문도 별도 Page로 펼쳤다.
- 같은 제목의 중복 Notion 페이지와 이미 저장된 Pages·Prompts는 수정하지 않고 스킵한다.
- Notion 원문의 이미지 블록은 Pages의 TipTap 이미지 노드로 보존해야 한다.
- 기존 `scripts/import-notion-week-20260803.mjs`의 Markdown→TipTap 및 로컬·Supabase 저장 흐름을 재사용한다.
- 신규 상위 Page 9건과 재사용 Prompt 117건을 로컬·Supabase에 저장했다.
- 페이지별 Prompt 수는 15·81·0·8·3·0·0·4·6개다.
- `노시언의 30가지 노션템플릿` 이미지는 만료되지 않는 PNG data URL로 저장했고, TipTap 이미지 노드 1개를 확인했다.
- 최초 리뷰에서 사라진 하위 Page·Database·unknown 참조를 Markdown 링크로 보존하도록 보정했다. 저장된 인덱스 링크는 로컬·Supabase 각각 55개다.
- 본문 지문은 TipTap 본문 구조 전체를 해시해 링크와 이미지가 다른 내용을 중복으로 오판하지 않는다.
- 최종 재실행은 로컬·Supabase 모두 Page 신규 0건·스킵 9건, Prompt 신규 0건·스킵 117건이었다.
- 전체 테스트 32개, 대상 스크립트 ESLint, 프로덕션 빌드가 통과했다.

## 그래프 엔지니어링 Notion 이관 메모

- 원문 URL은 `https://app.notion.com/p/3b5d85dc287580b4b259f036d6360c85?source=copy_link`다.
- 연결된 Notion MCP에는 공유되지 않아 404가 발생했지만 공개 `loadPageChunk` API로 읽을 수 있다.
- 제목은 `[그래프 엔지니어링]`이고 루트 본문 블록은 35개다.
- 본문 이미지 블록은 없고 Notion 기본 커버 `/images/page-cover/usda_cherries.png` 1개가 있다. Pages 본문 첫 이미지로 보존한다.
- `Plain Text` 코드 블록 3개는 설치 프롬프트, `/graph` 실행 예시, 검사 프롬프트이므로 Prompts 3건으로 분리한다.
- 기존 공개 Notion 블록 렌더링과 Markdown→TipTap 저장 패턴을 재사용하고 새 의존성은 추가하지 않는다.

## Pages 링크 클릭 회귀 메모

- 기존 저장 Page `15초 영상 프롬프트 클로드 스킬`에는 TipTap link mark 4개가 있어 데이터 누락이 아니다.
- 공통 `TiptapEditor`의 Link 확장이 `openOnClick: false`로 설정돼 모든 Pages 링크 이동을 끈 것이 원인이다.
- `openOnClick: true`로 수정하고 저장 DB의 link mark가 클릭 설정과 함께 유지되는 회귀 테스트를 추가했다.
- Notion의 탭 들여쓰기 이미지가 앞 문단에 흡수되던 공통 Markdown 경계도 수정해 원문 이미지 196개가 모두 TipTap image 노드로 보존된다.
- 최소 수정은 공통 Link 설정의 클릭 이동을 활성화하는 것이다.
- 회귀 검사는 link mark가 있는 저장 데이터와 클릭 설정을 함께 확인한다.

## 노시언 템플릿 하위 페이지 이관 결과

- 하위 Page 44개와 collection 2개를 별도 Pages로 저장했고, collection 행은 합계 508개다.
- 원문 토글 111개 내부 텍스트, 이미지 196개, 링크 1,301개(임베드·영상·파일 75개 포함)와 unknown bookmark/button 원문 위치 423개를 보존했다.
- 이미지 119개 고유 자산은 5분 만료 서명 URL 대신 data URL로 고정했다.
- 명시적 프롬프트는 오픈클로 2개와 AI 랭킹 Top5 1개만 분리했다.
- 로컬·Supabase 재실행은 Page 46개와 Prompt 3개 모두 신규 0건이며 저장 검증 수치가 같다.

## 원문

## /doctor Notion Page 이관 메모

- 원문 URL은 `https://app.notion.com/p/doctor-3a9d85dc287581cb8624cb6da49dd109?source=copy_link`다.
- Notion MCP 연결 워크스페이스에서는 404지만 공개 `loadPageChunk` API로 읽을 수 있다.
- 페이지 ID는 `3a9d85dc-2875-81cb-8624-cb6da49dd109`이고 제목은 `/doctor 쓰는 법 + 최적화 프롬프트`다.
- 공개 블록은 87개이며 표 3개, 코드 블록 3개다. 본문 이미지 블록은 없지만 페이지 커버와 `SocialMediaPreviewImage.png` 첨부가 있다.
- 커버와 첨부는 서명 URL을 그대로 저장하지 않고 data URL 이미지 노드로 저장해야 만료 후에도 보인다.
- 코드 블록 3개를 Prompts에 별도 저장한다. `/doctor`, CLAUDE.md 위치 확인, 지시문 다이어트 프롬프트다.
- 기존 Pages·Prompts 중복 판정과 로컬 `dev`, 운영 사용자 `f72e9a44-79d8-4061-a700-3ec50bb04a97` 저장 흐름을 재사용한다.
- `scripts/import-notion-doctor-page.mjs --check`는 블록 87개·표 3개·프롬프트 3개·이미지 2개·링크 3개를 확인한다.
- 로컬은 기존 Page 1건·Prompt 3건을 스킵했고 Supabase는 Page 1건·Prompt 3건을 추가했다. 재실행에서는 양쪽 모두 신규 0건이었다.
- 전체 테스트 82개와 일회성 `AUTH_SECRET` 환경변수를 둔 프로덕션 빌드가 통과했다. 기본 빌드는 기존 AUTH_SECRET 미설정으로 실패한다.

## 2026-08-15 Notion 링크 묶음 이관 메모

- 사용자 링크는 ChatGPT 원문이 2번 있어 고유 원문은 13개다.
- 기존 저장 Page는 ChatGPT 명령어, 브랜드 웹페이지 워크플로우, 코덱스 이미지 스튜디오, 그래프 엔지니어링, 나만의 나무위키, Session Wrap 총 6개다. 이들은 사용자 요구대로 갱신하지 않고 스킵한다.
- 신규 원문은 말로 앱 만들기, 클로드코드 스킬 10개, 클로드코드 MCP 10개, 클로드코드 플러그인 10개, 전문 AI 직원, 이미지 자동화 스킬팩, 세컨드브레인 스타터 키트 총 7개다.
- 공개 블록 기준 신규 이미지·커버는 전문 AI 직원 3개, 스킬·MCP·플러그인 각 커버 1개이며, 이미지 자동화·세컨드브레인에는 ZIP 첨부가 각 1개 있다.
- 신규 코드 블록은 말로 앱 만들기 10개, 전문 AI 직원 10개, 이미지 자동화 스킬팩 4개이며 총 24개를 Prompts로 분리한다.
- `scripts/import-notion-link-batch-20260815.mjs --check`는 기존 Page 6개와 중복 링크 1개를 제외하고 신규 7개를 검사했다. 신규 이미지 5개, ZIP 첨부 2개, 표 12개, 프롬프트 24개다.
- 신규 Page 7개·Prompt 24개를 로컬·Supabase 양쪽에 저장했고, 재실행에서는 신규 쓰기 0건이었다. ZIP은 ASCII Storage 내부 키와 한글 표시 파일명을 분리했다.

## 원문

- URL은 `https://miny.tistory.com/30`이다.
- 제목은 `UI 디자인 스타일 20가지 총정리 (플랫, 글래스모피즘, 뉴모피즘 등)`이다.
- 실제 본문 컨테이너는 `#article-content .tt_article_useless_p_margin`이다.
- 본문 컨테이너에는 이미지 21개, 링크 48개, `h2`·`h3` 29개가 있다.
- 글 상단 스킨 썸네일은 첫 본문 이미지와 같은 파일이므로 본문 기준 이미지 수는 21개다.
- 이미지 순서는 총정리 표지, 플랫, 머티리얼, 플루언트, 애플 휴먼 인터페이스, 글래스모피즘, 뉴모피즘, 미니멀리즘, 다크 모드, 카드 기반, 그라데이션, 타이포그래픽, 브루탈리즘, 네오브루탈리즘, 3D, 아이소메트릭, 클레이모피즘, 오로라 UI, 프루티거 에어로, 스큐어모피즘, AI 정리 고지다.
- 카카오 CDN URL에는 `expires`와 `signature`가 있어 그대로 저장하면 만료된다. `public/imports/ui-design-styles/`에 원본 21개를 보관하고 Pages 문서는 `/imports/ui-design-styles/...`를 참조한다.

## 프로젝트 흐름

## 다른 브라우저 로그인 후 북마크 미표시 진단 메모

- 사용자 증상은 다른 브라우저에서 로그인하고 저장소를 다시 받은 뒤 Bookmark 목록이 비어 보이는 현상이다.
- 먼저 인증 사용자 식별자, 북마크 API의 사용자 조건, 저장소 구현 선택 조건, 실제 Supabase·SQLite 레코드 존재를 분리해 확인한다.
- 이 진단에서는 코드 수정 없이 원인을 확정하고, 확인하지 못한 외부 상태는 추측하지 않는다.
- 코드상 북마크와 GitHub Star는 모두 `session.user.id`를 `user_id`로 사용하고, 목록 조회도 같은 값으로 필터링한다.
- 운영 Supabase `bookmarks`는 `user_id=f72e9a44-79d8-4061-a700-3ec50bb04a97`에 290건만 있다. `github_stars`는 같은 사용자 118건을 포함해 여러 사용자 ID로 총 1,000건이다.
- 로컬 `data/mymark.db`에는 `user_id=dev` 북마크 5건만 있고 Star는 0건이다.
- 로컬 앱 Dev Login을 실제 브라우저로 재현했을 때 세션 ID는 `dev`, 백엔드는 Supabase, `GET /api/bookmarks`는 200과 빈 배열이었다.
- Auth.js `@auth/core` OAuth 콜백은 Adapter가 없을 때 provider 계정과 별개로 `crypto.randomUUID()`를 `user.id`에 넣는다. 현재 `src/lib/auth.ts`에는 Adapter가 없어 GitHub 로그인 때마다 새 UUID가 생기는 것이 근본 원인이다.
- 실제 Supabase `oauth_tokens`에도 서로 다른 UUID 5개가 있고, `github_stars`도 여러 UUID로 분산되어 있어 같은 사용자의 브라우저별 재로그인 증상과 일치한다.
- 따라서 북마크 290건 자체는 보존되어 있으나 최초 이관에 사용한 `f72e9a44-79d8-4061-a700-3ec50bb04a97` UUID로만 조회된다. 안정적인 provider 계정 매핑과 기존 데이터의 canonical user ID 통합이 필요하다.
- GitHub 계정 `gilwon`의 provider account ID `15724434`를 기존 canonical ID `f72e9a44-79d8-4061-a700-3ec50bb04a97`에 연결하고, 다른 GitHub 계정은 `github:<providerAccountId>`로 격리한다.
- DB 행을 이동하지 않고 JWT의 `token.sub`만 안정 ID로 정규화해 기존 북마크 290건을 그대로 조회하도록 수정했다.
- `tests/auth-user-id.test.mjs`에서 기존 계정 호환과 다른 계정 격리를 회귀 검증한다.
- 기존 브라우저의 JWT도 `githubLogin=gilwon`이면 다음 요청에서 canonical ID로 복구해 로그아웃·재로그인 없이 기존 북마크를 다시 조회한다.

- Pages 본문은 `custom_pages.content`의 TipTap JSON이다.
- 기존 변환 함수는 `src/lib/markdown-to-tiptap.ts`의 `markdownToTiptapDoc`이다.
- 기존 URL 수집 함수는 `src/lib/url-to-markdown.ts`의 `fetchUrlAsMarkdown`이며 Readability와 Turndown을 이미 사용한다.
- 현재 Markdown 변환기는 독립 이미지 문법 `![alt](src)`을 이미지 노드로 처리하지 않아 URL 가져오기 결과에서 이미지를 잃는다.
- 최소 공통 수정은 독립 이미지 한 줄을 `{ type: "image", attrs: { src, alt } }`로 바꾸는 분기와 회귀 테스트다.
- 일회성 가져오기 스크립트는 기존 `scripts/import-*-page.mjs`처럼 `.env.local`, SQLite `dev`, Supabase 사용자 `f72e9a44-79d8-4061-a700-3ec50bb04a97`를 사용한다.
- 다른 사용자의 미추적 스크립트와 `DESIGN-apple.md`는 건드리지 않는다.

## 완료 판정

- `--check` 같은 무변경 검사에서 제목, 이미지 21개, 원문 링크가 확인된다.
- 저장된 TipTap JSON에서 이미지 노드 21개가 모두 로컬 경로이고 링크 마크가 원문 링크를 보존한다.
- 같은 스크립트를 다시 실행해도 중복 페이지가 생기지 않는다.
- 테스트, 린트, 빌드 후 실제 `/pages/{id}`에서 본문 이미지가 표시된다.

## 구현·검증 상태

- `scripts/import-ui-design-styles-page.mjs --check`는 DB를 열지 않고 원문 제목, 이미지 21개, 링크 48개, 로컬 자산 21개, TipTap 이미지 21개, 링크 mark 48개, 표 4개를 검사한다.
- Pages 문서의 이미지는 `/imports/ui-design-styles/` 아래 로컬 자산을 참조한다. 운영 화면에서 보이려면 이 코드와 자산을 먼저 배포해야 한다.
- 실제 SQLite·Supabase 저장과 프로덕션 배포는 실행하지 않았다.
- 대상 테스트와 전체 테스트, 빌드는 통과했다. 전체 린트는 변경 파일 밖 기존 오류 23개 때문에 실패했으며 변경한 코드 파일만 검사하면 통과한다.

## ADU GitHub Picks 이관 메모

- 원문은 `https://adu-github-picks.vercel.app/`이며 제목은 `클로드 성능을 바꾸는 깃허브 무료 도구 6개`다.
- 원문에는 카파시의 4원칙, Anthropic 공식 스킬, Superpowers, Matt Pocock 스킬, The Agency, ECC를 다룬다.
- `❓ 자주 묻는 질문` 섹션은 Page에서 제외한다.
- 설치·사용 예시 프롬프트는 도구별 2·3·2·4·3·2개, 총 16개를 Prompt로 분리한다.
- Prompt 카테고리는 `Claude · GitHub 도구`로 한다.

## Notion ChatGPT 명령어 이관 메모

- 원문 URL은 `https://app.notion.com/p/ChatGPT-3affd99f0e5f819d8623cf3d2cc86cc6?source=copy_link`이다.
- Notion 블록 API 기준 제목은 `ChatGPT 명령어·단축어 총정리`다.
- FAQ 섹션은 확인되지 않았으며, 본문에는 비공식 단축어 76개와 공식 명령어 표 25행이 있다.
- Prompt는 비공식 단축어 76개와 공식 명령어 24개를 재사용 항목으로 분리해 총 100개로 저장한다.
- Prompt 카테고리는 `ChatGPT · 명령어·단축어`로 한다.

## 최종 검증

- ADU GitHub Picks는 로컬·Supabase 모두 Page 1건과 Prompt 16건을 추가했고 재실행 시 전부 스킵했다.
- Notion 페이지는 로컬 Page 1건·Prompt 100건이 이미 있어 스킵했고, Supabase에는 Page 1건과 Prompt 100건을 추가한 뒤 재실행 기준으로 확인했다.
- 저장된 두 Page와 Prompt 전체에서 `자주 묻는 질문`·`FAQ` 문자열을 찾지 못했다.
- `npm test` 20개 통과, 변경 스크립트 ESLint 통과, `AUTH_SECRET` 일회성 주입 빌드 통과.

## 2026년 8월 첫째 주 Notion 이관 메모

- 이번 주는 서울 시간 기준 2026-08-03 00:00부터 현재 날짜인 2026-08-06까지다.
- Notion 검색은 현재 연결 사용자인 `GILWON YOO`가 직접 생성한 페이지로 제한했다.
- 여러 검색어 결과의 합집합으로 확인한 후보는 8건이다.
- 후보는 `AI 콘텐츠 자동화 아이디어 프롬프트｜ChatGPT부터 바이브코딩까지`, `클로드·ChatGPT 연동 앱 TOP5`, `AI 직원 200명 공짜로 부리는 법 (전체 정리본)`, `NAVER 키워드챌린지 자동 수집기`, `과잉 지정 체크리스트 — 클로드 제작자가 말한 지시 줄이는 법`, `💚키워드 확장 프롬프트+키챌 자동수집기`, `직장인들을 위한 무료 오픈소스 TOP 3`, `업무시간 단축시켜주는 ChatGPT 프롬프 40가지`다.
- 로컬 DB에는 앞선 3건 가운데 `AI 콘텐츠 자동화 아이디어 프롬프트｜ChatGPT부터 바이브코딩까지`와 `AI 직원 200명 공짜로 부리는 법 (전체 정리본)`이 확인됐다. 전체 로컬·Supabase 중복은 실행 전에 다시 조회한다.
- Page 중복은 사용자별 제목으로, Prompt 중복은 사용자별 제목과 카테고리 조합으로 판정한다.
- 사용자 요청에 따라 기존 중복 데이터는 갱신하지 않고 그대로 스킵한다.
- 기존 `scripts/import-notion-recent-ai-pages.mjs`의 Markdown→TipTap 변환과 로컬·Supabase 저장 패턴을 재사용하고 새 추상화나 의존성은 추가하지 않는다.
- 완료 기준은 후보 8건이 기존 항목 포함 Pages에 존재하고, 본문 안 재사용 프롬프트가 Prompts에 존재하며, 동일 스크립트 재실행에서 추가·수정 0건을 확인하는 것이다.
- 페이지별 Prompt 수는 12·14·4·0·2·2·0·82개로 총 116개다.
- 첫 실행은 로컬·Supabase 모두 Page 신규 6건·스킵 2건, Prompt 신규 100건·스킵 16건이었다.
- Advisor 재실행은 로컬·Supabase 모두 Page 신규 0건·스킵 8건, Prompt 신규 0건·스킵 116건이었다.
- 로컬 DB에서 대상 Page 제목별 1건과 Prompt 카테고리 합계 116건을 확인했다.
- 변경 스크립트 ESLint, 전체 테스트 32개, 프로덕션 빌드가 통과했다.

## PDF 목록 정렬 기능 메모

- 대상은 `src/components/pdf-viewer/pdf-viewer.tsx`의 저장된 PDF 목록이다.
- 기본값은 이름 가나다순 오름차순이며 두 번째 선택지는 등록일 최신순이다.
- 기존 공통 `Select`, `compareTitleAsc`, `compareIsoDesc`를 재사용하고 새 의존성은 추가하지 않는다.
- 정렬은 클라이언트의 원본 `savedPdfs`를 복사한 파생 목록에만 적용해 API·저장·삭제 흐름을 바꾸지 않는다.
- 이름 동률은 등록일 최신순, 등록일 동률은 이름 가나다순으로 결정하며 등록일이 없는 항목은 등록일순에서 뒤로 보낸다.
- 완료 기준은 기본 이름순, 정렬 전환, 저장·삭제 뒤 선택 유지, 변경 파일 린트·전체 테스트·빌드 통과다.
- `sortSavedPdfs`가 원본 배열을 복사해 이름순 또는 등록일 최신순으로 정렬하고, 화면은 선택 상태와 파생 목록을 연결한다.
- 대상 테스트 2개와 전체 테스트 32개, 변경 파일 ESLint, 프로덕션 빌드, Standards·Spec 코드 리뷰가 통과했다.
- 실제 브라우저는 인증되지 않은 세션에서 `/login`으로 이동해 480px·1024px·1440px 렌더링 검증을 수행하지 못했다.

## PDF 클라우드 저장 설계 메모

- `pdf-files` 비공개 Supabase Storage 버킷을 사용하며 PDF MIME만 허용하고 파일당 20MB로 제한한다.
- Auth.js 사용자 ID를 서버에서 안전한 폴더 키로 바꾸고 모든 목록·열기·삭제 경로를 해당 폴더로 고정한다.
- 서버가 service role로 2시간짜리 서명 업로드 토큰을 발급하고 브라우저는 공개 anon 키로 Storage에 직접 업로드한다.
- 저장 목록은 최근 100개로 제한하며 선택한 파일은 짧은 만료 시간의 서명 URL로 연다.
- API 경계에서 인증, UUID 식별자, 파일명·용량·MIME를 검증한다.
- 검증 경계는 서명 URL 발급, 저장 목록, 사용자 경로 격리, 열기, 삭제와 실제 브라우저 업로드 흐름이다.
- 실제 Storage 업로드 후 새로고침에서도 원본 파일명이 유지됐고, 서명 URL 열기와 삭제를 확인했다.
- 480px·1024px·1440px에서 가로 넘침이 없고 모바일 작업 버튼은 44px이며 키보드 파일 선택이 동작한다.
- 전체 테스트 28개, 변경 파일 ESLint, 프로덕션 빌드와 TypeScript 검사가 통과했다.

## PDF 중복 저장 버그 메모

- 동일 파일은 파일명이 아니라 PDF 바이트 내용이 같은 경우로 정의한다.
- 같은 파일명이라도 내용이 다르면 별도 파일로 저장한다.
- 중복 판정 범위는 인증된 사용자 자신의 `pdf-files` 폴더로 제한한다.
- 기존 파일을 덮어쓰거나 삭제하지 않고 새 저장만 거절한다.
- 원인은 매 저장마다 UUID 경로를 만들어 `upsert: false`가 같은 내용의 파일을 충돌로 판단하지 못한 것이다.
- 신규 파일은 SHA-256 지문을 object name으로 사용해 두 탭 동시 저장도 Storage 충돌로 한 건만 남긴다.
- 기존 UUID 형식은 최근 100개를 서버에서 해시해 중복을 검사하고 열기·삭제 호환을 유지한다.
- 실제 브라우저에서 순차 중복은 1→1, 같은 이름의 다른 내용은 1→2, 동시 저장은 성공 1건·중복 1건을 확인했다.
- 전체 테스트 30개, 변경 파일 ESLint, 프로덕션 빌드와 TypeScript 검사가 통과했다.

## PDF 뷰어 구현 메모

- `/pdf-viewer`는 인증된 앱 레이아웃 안의 새 메뉴로 제공한다.
- 업로드한 PDF는 `URL.createObjectURL`로 브라우저에서만 미리 보고 서버로 전송하거나 저장하지 않는다.
- 네이티브 파일 입력과 브라우저 PDF 렌더러를 사용하며 새 의존성은 추가하지 않는다.
- 검증 경계는 실제 `/pdf-viewer` 화면의 파일 선택·드래그 앤 드롭·오류·교체·초기화 동작이다.
- 실제 PDF의 즉시 미리보기와 파일명·용량 표시, 초기화, 드래그 앤 드롭을 확인했다.
- 확장자만 `.pdf`인 일반 파일은 파일 앞부분의 `%PDF-` 서명을 확인해 거절한다.
- 480px·1024px·1440px에서 가로 넘침이 없고 모바일 메뉴에서도 PDF 뷰어 링크가 표시된다.
- 키보드 포커스 순서와 WCAG 2 A·AA 자동 검사 위반 0건을 확인했다.
- 전체 테스트 22개, 변경 파일 ESLint, 프로덕션 빌드와 TypeScript 검사가 통과했다.

## GitHub Star 동기화 번역 조사 메모

- 동기화는 `src/app/api/stars/sync/route.ts`에서 GitHub 목록을 받은 뒤 `src/lib/github.ts`의 `upsertStars`로 저장한다.
- 신규·기존 항목 모두 `src/lib/star-translation.ts`의 `withKoreanTranslation`을 통과한다.
- 현재 번역 함수는 `src/data/star-descriptions-ko.json`의 정적 매핑만 사용하며, 매핑에 없는 레포는 영문 설명을 그대로 반환한다.
- 실제 증상과 같은 실패 테스트를 먼저 만든 뒤 공통 번역 함수만 최소 수정한다.
- 운영 데이터에서 설명이 있는 영문 전용 Star 20개가 모두 정적 매핑 밖인 것을 확인했다.
- 누락된 20개 정적 번역과 `DoHyun468/claw-hwp` 회귀 테스트를 추가했다.
- 대상 테스트 5개와 전체 테스트 21개, 변경 테스트 ESLint, 프로덕션 빌드가 통과했다.
- 화면에 영문으로 남은 항목은 번역표와 테스트만 수정하고 운영 DB 백필을 실행하지 않아 발생했다.
- 운영 DB에는 정적 매핑과 일치하지만 영문 설명만 저장된 항목이 20개 남아 있었다.
- `scripts/add-korean-star-descriptions.mjs`로 운영 DB 20개를 백필했고, 재실행 시 갱신 0개와 매핑 대상 영문 전용 0개를 확인했다.

## dasolin GitHub 링크 이관 조사 메모

- 원문은 `https://dasolin.net/tips/github-links`이며 로그인 후 동적 목록을 표시한다.
- 원문 공개 데이터는 숨김 항목 제외 120개다. GitHub 117개, Hugging Face 1개, 기타 링크 2개다.
- 카테고리는 클로드 87개, 기타 19개, 코덱스 6개 등 11개 문자열로 구성된다.
- 외부 서비스 장애와 로그인에 의존하지 않도록 링크 목록은 프로젝트 정적 데이터로 보관한다.
- 상단 메뉴에는 `GitHub 링크` 한 항목만 추가하고, 전용 페이지에서 검색·출처·카테고리 필터와 모든 설명·링크를 제공한다.
- 원문 장문은 복제하지 않고 링크와 의미를 보존한 자체 한국어 설명을 사용한다.

### GitHub 링크 구현 및 검증 결과

- 정적 데이터 120개와 고유 HTTPS 링크 120개를 저장했으며 GitHub 117개, Hugging Face 1개, 기타 2개다.
- 검색, 출처 필터, 카테고리 필터와 상단 GitHub 링크 메뉴를 추가했다.
- 테스트 22개, 변경 파일 ESLint, 프로덕션 빌드가 통과했다.
- 480px·1024px·1440px에서 가로 넘침 없이 각각 1열·3열·3열로 표시된다.
- 검색과 필터 결과, 키보드 탐색, WCAG 2 A·AA 자동 검사를 확인했고 접근성 위반은 0건이다.

## Netlify ChatGPT 트레이딩 모드 이관 메모

- 원문은 `https://resilient-centaur-bab838.netlify.app/`이며 HTML 제목은 `ChatGPT 트레이딩 모드 — 직접 만들어 쓰는 AI 투자 판단 시스템`이다.
- 본문에는 SET A·B·C 인스트럭션 3개와 코어 프롬프트 P01~P07 7개, 총 10개의 티켓형 프롬프트가 있다.
- Prompt 카테고리는 `ChatGPT · 트레이딩 모드`로 한다.

## Netlify 최종 검증

- 로컬·Supabase에 Page 1건과 Prompt 10건을 추가했고 재실행 시 모두 스킵했다.
- 저장된 Page와 Prompt 전체에서 `자주 묻는 질문`·`FAQ` 문자열을 찾지 못했다.
- `npm test` 20개 통과, 변경 스크립트 ESLint 통과, `AUTH_SECRET` 일회성 주입 빌드 통과.

## Instagram 이미지 프롬프트 이관 메모

- 원본은 `https://www.instagram.com/p/DblF3yboIWw/`이며 슬라이드 16장을 확인했다.
- 프롬프트가 포함된 슬라이드 4·6·8·10·12·14의 내용만 Prompt로 분리한다.
- Prompt 카테고리는 `ChatGPT · 이미지 프롬프트`로 한다.
- 이미지 설명·전자책 홍보·아웃트로 슬라이드는 저장하지 않는다.

## Instagram 최종 검증

- 로컬·Supabase에 Prompt 6건을 추가했고 재실행 시 각각 6건 모두 스킵했다.
- 저장된 Prompt 6건 모두 원본 Instagram 링크를 포함하고, FAQ 문자열과 빈 본문은 없었다.
- `npm test` 20개 통과, 변경 스크립트 ESLint 통과, `AUTH_SECRET` 일회성 주입 빌드 통과.

## Codex 훅 code 127 진단 메모

- 증상은 SessionStart 훅은 성공하지만 UserPromptSubmit과 PreToolUse 훅이 모두 종료 코드 127로 실패하는 것이다.
- 종료 코드 127은 셸이 실행할 명령을 찾지 못했을 때 흔히 반환하지만, 실제 훅 명령과 실행 환경을 확인하기 전에는 원인으로 확정하지 않는다.
- 설정은 수정하지 않고 동일 훅을 직접 실행해 재현한 뒤 누락된 명령 또는 PATH 차이를 특정한다.
- `~/.codex/hooks.json`의 UserPromptSubmit과 PreToolUse는 모두 `/Users/gilwon/Library/Application Support/Muxy/hooks/muxy-codex-hook.sh`를 호출한다.
- 해당 파일과 Muxy 설치 경로가 존재하지 않으며, 두 명령을 그대로 실행하면 모두 `no such file or directory`와 종료 코드 127이 재현된다.
- `node`, `zsh`, `sh` 실행 파일은 정상 확인되어 PATH 문제가 아니다. 삭제된 Muxy의 훅 등록만 설정에 남은 것이 원인이다.

## 외부 문서 11건 Pages·Prompts 이관 메모

- 첫 원문은 `https://qjc.app/blog/claude-code-prompt-caching`이며 쿼리 문자열의 토큰은 저장하지 않는다.
- 첫 문서 제목은 `프롬프트 캐싱 완벽 가이드: Anthropic이 공개한 Claude Code의 비용 최적화 비밀`이다.
- QJC 서버 렌더링 본문은 Markdown 7,415자, 제목 계층 23개, 외부 링크 8개이며 본문 이미지는 없다.
- 둘째 원문은 `https://fieldby.notion.site/AI-9-3abd730b39538120ad44e38da71eaaa9`이다.
- 둘째 문서 제목은 `구글 무료 AI 툴 9개 총정리`이며 공개 Notion API 기준 블록 61개, 도구 섹션 9개, 북마크 9개다.
- 셋째 원문은 `https://app.notion.com/p/3b1c601852d1809e993cf1dd14e637aa`이며 쿼리 문자열은 저장하지 않는다.
- 셋째 문서 제목은 `나만의 플레이리스트 빌더 — 프롬프트 & 가이드`이고 재귀 수집 기준 블록 49개다.
- 셋째 문서의 `code` 블록 3개는 Spotify 자동 생성, YouTube Music 자동 생성, 연결 없이 목록 생성 프롬프트다.
- 넷째 원문은 `https://app.notion.com/p/30-Suno-3a072cc75bf68110b219f73cfbda69f4`이며 제목은 `30분 실습 — Suno로 첫 자작곡 초안 만들기`다.
- 넷째 문서는 블록 61개와 `code` 블록 5개를 포함한다. 장면 입력, 가사 초안, 스타일, 제외 스타일 4개는 Prompts로 분리하고 선택 이유 기록 양식은 Page에만 둔다.
- 다섯째 원문은 `https://app.notion.com/p/AI-3a072cc75bf681488106d4e9f17a91ab`이며 제목은 `AI 음악 시작 전 저작권 체크리스트 — 커버 대신 자작곡으로 출발하기`다.
- 다섯째 문서는 블록 58개이며 `code` 블록 1개는 증빙 폴더 목록이므로 Prompt로 분리하지 않는다.
- 여섯째 원문은 `https://app.notion.com/p/AI-6-3a072cc75bf681b78838f45126566a53`이며 제목은 `음치·초보도 AI 음악 채널을 시작할 수 있을까? 현실 답변 6가지`다.
- 여섯째 문서는 블록 44개이며 재사용 프롬프트 블록은 없다.
- 추가 목록 8건 중 Suno 실습·저작권·입문 FAQ 3건은 앞선 요청과 중복이므로 신규 대상으로 다시 세지 않는다.
- 일곱째 문서 제목은 `클릭되는 AI 썸네일 만들기 — 실패를 줄이는 5가지 규칙`이며 블록 52개다. `code` 블록 2개는 썸네일 공식과 발라드 예시 Prompt로 분리한다.
- 여덟째 문서 제목은 `AI 음악을 유튜브에 올릴 때 확인할 4가지 — 고지·권리·독창성·검수`이며 블록 62개와 재사용 프롬프트 0개다.
- 아홉째 문서 제목은 `혼자 운영하는 AI 콘텐츠 팀 만들기 — 역할 분담표와 지시문`이며 블록 50개다. 역할 카드 `code` 블록 1개를 Prompt로 분리한다.
- 열째 문서 제목은 `조회수 오른 영상 분석법 — CTR·30초·지속률 진단표`이며 블록 56개다. `code` 블록은 성공 영상 기록 양식이므로 Page에만 둔다.
- 열한째 문서 제목은 `AI에게 맡기면 안 되는 5가지 — 자동화해도 내 브랜드를 지키는 법`이며 블록 49개와 재사용 프롬프트 0개다.
- 기존 `markdownToTiptapDoc`, SQLite `dev`, Supabase 사용자 `f72e9a44-79d8-4061-a700-3ec50bb04a97` 저장 패턴을 재사용한다.
- 각 요청 문서의 본문만 저장하고 관련 하위 페이지 내용은 중복 삽입하지 않는다. 앱 코드는 수정하지 않는다.
- 완료 기준은 고유 제목 11개와 프롬프트 10개가 로컬·Supabase에 각 1건 존재하고, 중복 URL 3개는 추가 생성되지 않으며 재실행 시 추가·수정 0건인 것이다.

### 외부 문서 이관 구현·검증 결과

- 첫 확장 실행은 로컬·Supabase 모두 기존 Page 3건과 Prompt 3건을 스킵하고 신규 Page 8건과 Prompt 7건을 추가했다.
- 재실행은 양쪽 모두 Page 11건과 Prompt 10건을 스킵했고 추가·수정은 0건이었다.
- Advisor 조회에서 양쪽 모두 고유 Page 제목 11개와 Prompt 제목 10개가 확인됐고 원문 URL의 query와 전달받은 토큰은 저장되지 않았다.
- TipTap 본문에서 코드 블록, 표, 목록과 원문 링크가 확인됐다. 관련 하위 페이지 본문은 중복 삽입하지 않았다.
- 기본 Node 22는 기존 `better-sqlite3` ABI와 맞지 않아 DB를 열기 전에 실패했고, 설치 바이너리와 맞는 Node 26으로 저장과 검증을 완료했다.
- 변경 스크립트 ESLint와 전체 테스트 32개가 통과했다.
- 첫 빌드는 프로덕션 필수 `AUTH_SECRET` 부재로 실패했고, 일회성 검증 값을 주입한 빌드는 TypeScript와 정적 페이지 31개 생성을 포함해 통과했다.
- Spec 재검토에서 인라인 링크 보존을 확인했고 Standards·Spec 리뷰 모두 통과했다.

## Session Wrap·Daily Wrap Pages·Prompts 이관 메모

- 원문은 `https://app.notion.com/p/10-Session-Wrap-Daily-Wrap-3b58b06f1cb9802c842ddb6a813ee6cf`이며 query는 저장하지 않는다.
- 제목은 `10초면 클로드가 똑똑해져요 — Session Wrap + Daily Wrap 설치 가이드`다.
- 공개 Notion API 기준 블록 157개, 루트 하위 블록 156개이며 누락 블록은 없다.
- 코드 블록 22개 중 Session Wrap·Daily Wrap 스킬 지시문 2개, 설정 병합 요청 1개, 민감 세션 제외 요청 1개를 Prompt로 분리한다.
- 셸·JSON·파일 구조·슬래시 명령 예시는 실행 자료이므로 Page에만 보존한다.
- 기존 이관 스크립트와 중복 판정 규칙을 재사용하며 앱 코드는 수정하지 않는다.
- 완료 기준은 Page 1건과 Prompt 4건이 로컬·Supabase에 각 1건 존재하고 재실행 시 추가·수정 0건인 것이다.

### Session Wrap·Daily Wrap 이관 결과

- 첫 실행은 로컬·Supabase 모두 기존 Page 11건과 Prompt 10건을 스킵하고 신규 Page 1건과 Prompt 4건을 추가했다.
- 재실행은 양쪽 모두 Page 12건과 Prompt 14건을 스킵했고 추가·수정은 0건이었다.
- Advisor 조회에서 양쪽 Page 본문 198개 TipTap 노드와 Prompt 본문 길이 2,287·1,748·150·36자가 동일했다.
- query가 제거된 원문 URL과 필수 본문, Prompt category·요약·사용 시점을 확인했다.
- 자체 검증은 한 차례 Notion 429로 실패했으나 재시도에서 블록 157개·Prompt 4개를 포함해 통과했다.
- 변경 스크립트 문법·ESLint, 전체 테스트 32개, 프로덕션 빌드와 Standards·Spec 리뷰가 통과했다.

## MCP 서버 50선 Notion 이관 메모

- 원문은 `https://exultant-principle-9c5.notion.site/MCP-50-3b791cb23c4d81d9b3a6ca2373d61606`이다.
- 기존 `markdownToTiptapDoc`, SQLite `dev`, Supabase 사용자 `f72e9a44-79d8-4061-a700-3ec50bb04a97` 저장 패턴을 재사용한다.
- 이미지와 링크 표시명을 보존하고, 연결된 하위 페이지는 원문 확인 후 별도 Page 이관 여부를 결정한다.
- 완료 기준은 양 DB에 동일 Page·Prompt가 존재하고 재실행 시 추가·수정 0건이며 전체 테스트와 빌드가 통과하는 것이다.

### MCP 서버 50선 이관 결과

- 공개 Notion API에서 전체 122블록과 루트 하위 118블록을 확인했다.
- 실제 원문에는 하위 Page·토글·이미지가 없고, 코드 프롬프트 50개와 표시명 있는 북마크 링크 2개가 있다.
- 로컬·Supabase에 Page 1건과 Prompt 50건을 저장했고, 재실행은 Page 1건·Prompt 50건을 모두 스킵했다.
- 저장 Page는 코드 블록 50개, 원문 링크 1개와 북마크 링크 2개를 보존한다.
- 전체 테스트 37개, 대상 ESLint, 빌드와 Standards·Spec 리뷰가 통과했다.
- 완료 시점의 추가 Supabase 직접 조회는 세 차례 네트워크 타임아웃으로 중단했으며, 최초 저장과 재실행 당시의 저장 직후 검증 결과는 양 DB 모두 일치했다.

## 2026-08-09~10 Notion 이관 메모

- 날짜 범위는 서울 시간 기준 2026-08-09 00:00부터 2026-08-11 00:00 직전이다.
- 연결 사용자는 `GILWON YOO`이며 본인이 생성한 페이지로 검색을 제한했다.
- 여러 검색어 결과의 합집합으로 확인한 후보는 `핫한 깃허브 클로드 스킬 TOP 10 · 설치법 정리본`, `GPT 해킹 프롬프트 공개` 2건이다.
- 후보 생성 시각은 각각 2026-08-09 15:56, 2026-08-10 06:30 서울 시간이다.
- 기존 Page 사용자별 제목, Prompt 사용자별 제목·카테고리 중복 판정과 `markdownToTiptapDoc` 변환 패턴을 재사용한다.
- 서명 만료가 있는 Notion 이미지 URL은 저장하지 않고 프로젝트의 영구 자산으로 보존한다.
- 완료 기준은 신규 항목만 로컬·Supabase에 저장하고, 재실행 신규 0건과 저장 이미지 표시를 확인한 뒤 전체 테스트·대상 린트·빌드를 통과하는 것이다.

### 2026-08-09~10 Notion 이관 결과

- Notion MCP `notion_fetch` 원문에서 첫 Page 5,396자, 둘째 Page 9,638자를 직접 확보했다. wrapper 메타 문구와 `<empty-block/>`만 제거했고, 둘째의 `attachment:...:GPT.jpeg` icon 메타데이터는 본문에 저장하지 않았다.
- 첫 Page의 설치 도움 요청 1개, 둘째 Page의 복사-붙여넣기 프롬프트 23개·흔한 문제 질문 7개·마스터 프롬프트 1개를 분리해 Prompt 32개를 저장했다. 사용 예시는 Prompt로 저장하지 않았다.
- 정상 첫 실행은 로컬·Supabase 각각 Page 2건과 Prompt 32건을 저장했고, 재실행은 양쪽 신규 Page·Prompt 0건이었다.
- 직접 DB 조회에서 양쪽 Page 2건·Prompt 32건, 빈 본문 0건, 원문 링크 누락 0건, 이미지 노드 0건, 만료 서명 URL 0건, Prompt 제목·카테고리 중복 0건을 확인했다.
- 대상 ESLint, 전체 테스트 37개, `AUTH_SECRET` 일회성 주입 프로덕션 빌드가 통과했다.

## 루나 MAX Notion Page 이관 메모

- 원문은 `https://app.notion.com/p/MAX-3b3c601852d180daa9f3daa4e20e7a0c`이고 제목은 `‘루나 MAX’ 플러그인 가이드`다.
- Notion 연결 API는 워크스페이스 접근 권한이 없어 404였고, 로그인된 Chrome의 렌더링 본문에서 내용을 확보했다.
- 본문은 설치 명령 1개, Luna 작업 지시문 1개, 앱·CLI Max 설정, Native·Luna 비교표와 설명으로 구성된다.
- 공식 GitHub 링크는 `https://github.com/DannyMac180/sol-advisor`이며 원문 링크와 함께 보존한다.
- 설정 화면 이미지 3개는 Notion 인증 URL 대신 DB에 영구적인 data URL로 저장한다.
- 기존 `markdownToTiptapDoc`, SQLite 사용자 `dev`, Supabase 사용자 `f72e9a44-79d8-4061-a700-3ec50bb04a97` 패턴을 재사용한다.
- 완료 기준은 양 DB에 동일 제목 Page가 각 1건 존재하고 코드 블록 2개·표 1개·이미지 3개·원문과 GitHub 링크가 보존되며 재실행에서 신규 0건인 것이다.

### 루나 MAX Notion Page 이관 결과

- 첫 실행은 로컬·Supabase에 Page 각 1건을 추가했고, 같은 로직 재실행은 양쪽 모두 신규 0건·스킵 1건이었다.
- Advisor 직접 조회에서 양 DB content가 동일하고 Prompt는 0건이며, 코드 블록 2개·표 1개·이미지 3개·필수 링크 3개를 확인했다.
- 이미지 3개는 WebP data URL이며 원본 합계 72,900바이트를 보존한다.
- 운영 Pages 화면에서 코드 블록 2개·표 1개·GitHub 링크 1개와 자연 크기로 로드된 이미지 3개를 확인했다.
- 전체 테스트 37개와 `AUTH_SECRET` 일회성 주입 프로덕션 빌드가 통과했다.

## GitHub Star 미번역 설명 추가 메모

- 운영 `github_stars`에서 설명이 있고 한글이 전혀 없는 항목은 42개다.
- 기존 `src/data/star-descriptions-ko.json` 정적 번역표와 `scripts/add-korean-star-descriptions.mjs` 백필 흐름을 그대로 재사용한다.
- 앱 로직, 스키마, 의존성은 변경하지 않는다.
- 완료 기준은 42개 번역 추가, 회귀 테스트·전체 테스트·빌드 통과, 운영 DB 영문 전용 설명 0개 확인이다.
- 정적 번역 42개를 추가해 전체 번역표는 207개가 됐다.
- 첫 백필에서 이미 한국어인 `DoHyun468/claw-hwp`까지 포함해 43건이 갱신돼 해당 1건을 원문으로 복원했다.
- 백필 스크립트는 한국어가 포함된 설명을 건너뛰며, 재실행 갱신 0건과 운영 DB 영문 전용·한국어 중복 설명 0건을 확인했다.
- 전체 테스트 38개, 변경 파일 ESLint, 프로덕션 빌드와 Standards·Spec 리뷰가 통과했다.

## agent-docs 저장 중복 차단 메모

- 현재 업로드 목록 화면만 기존 문서와 초안 배치의 중복을 제외한다.
- 수동 신규 작성과 저장 API 직접 호출은 `POST /api/agent-docs`에서 중복 검사 없이 삽입된다.
- 목록 조회 기본값은 번들 본문을 비우므로 다중 파일 문서 비교에는 `full: true`가 필요하다.
- 기존 `docFingerprint`의 파일명·본문 완전 일치 규칙을 재사용한다.
- 최소 수정은 POST 저장 직전 사용자별 기존 전체 문서 지문과 비교하고 중복이면 409를 반환하는 것이다.
- 완료 기준은 중복 저장 거절, 내용이 다른 문서 정상 저장, 오류 문구 표시와 전체 테스트·빌드 통과다.
- 저장 API는 사용자별 파일 지문으로 결정적 UUID 슬롯을 사용해 순차·동시 중복을 모두 409로 거절한다.
- 편집으로 최초 지문 슬롯이 다른 내용에 점유돼도 다음 슬롯에서 신규 문서를 저장한다.
- 실제 Supabase HTTP에서 순차 201→409, 다른 본문 201, 동시 요청 201/409를 확인했다.
- A 생성→B 편집→A 재등록은 201이고 이어지는 A 중복은 409였다. 편집 이력 뒤 동시 재등록도 201/409였다.
- 검증용 문서는 모두 삭제했고 잔여 0건을 확인했다.
- 대상 테스트 14개, 전체 테스트 43개, route·lib·test ESLint와 프로덕션 빌드가 통과했다.
- 편집기 단독 ESLint의 기존 React refs·effect 오류 9건은 이번 변경 줄 밖이며 수정하지 않았다.

## 김효율 스킬팩 Notion Pages 이관 메모

- 원문 URL은 `https://sparkly-room-cb6.notion.site/3b5003c7f7be80bbb275eda06077f238`다.
- 공개 페이지 제목은 `김효율 스킬팩`이고 Jina Reader 기준 Markdown 본문은 7,661바이트다.
- 본문은 `intranet-style-skill-20260807.zip`, `ui-inspector-skill-20260807.zip` 설치와 사용법, 이용 범위·저작권 안내로 구성된다.
- Notion 원본은 지연 로딩 자식 1개를 포함해 104개 블록이며 실제 이미지 블록은 없다.
- ZIP 2개는 원문의 배포 제한과 만료되는 서명 URL 때문에 파일명·용량·원문 링크만 보존했다.
- 로컬·Supabase에 각각 Page 1건을 저장했고, 재실행은 양쪽 모두 신규 0건·중복 스킵 1건이었다.
- 저장 본문은 코드 10개, callout 7개, 원문 링크 3개, Claude Code 링크 1개와 마지막 라이선스 연락처를 포함하며 만료 URL은 없다.
- 전체 테스트 43개, 대상 ESLint와 프로덕션 빌드가 통과했다.

## 김효율 스킬팩 첨부 다운로드 보완 메모

- 사용자가 지정한 원문은 `https://app.notion.com/p/gilwon/5a5b256827ac8287b9b381e50f142820`이며 제목과 본문은 기존 `김효율 스킬팩` Page와 같다.
- 기존 이관 스크립트는 ZIP 2개의 만료되는 Notion 서명 URL을 저장하지 않았지만, 다운로드 링크도 모두 공개 Notion 원문으로 바꿔 실제 파일을 받을 수 없다.
- 회수한 원본은 `intranet-style-skill-20260807.zip` 114,372바이트와 `ui-inspector-skill-20260807.zip` 11,853바이트이며 둘 다 ZIP 시그니처 `PK\u0003\u0004`를 가진다.
- 원문은 파일 자체의 재배포를 금지하므로 `public` 자산이나 저장소에 ZIP을 커밋하지 않는다.
- ZIP은 사용자별 비공개 Supabase Storage에 저장하고 Pages 링크는 인증·소유 경계를 통과한 뒤 짧은 서명 URL로 이동한다.
- 테스트 seam은 사용자가 클릭하는 첨부 URL에 들어가는 식별자·파일명 검증과 사용자별 Storage 경로 생성이다.
- 완료 기준은 기존 Page 본문의 ZIP 링크 2개가 인증 다운로드 URL을 가리키고 로컬·운영 Page에서 두 ZIP의 파일명·크기·시그니처를 확인하는 것이다.
- 비공개 `page-attachments` 버킷과 사용자별 객체 경로를 사용하며 저장소와 공개 자산에는 ZIP을 추가하지 않았다.
- 다운로드 경로는 로그인·허용된 원문 ID·정확한 파일명을 검증하고 60초 서명 URL로 이동한다.
- 이관 대상은 정확히 일치하는 제목 1건만 갱신하며, 중복 제목이나 원문 링크만 인용한 별도 Page가 있으면 쓰기 전에 중단한다.
- 로컬 Pages 화면에 ZIP 링크 2개가 렌더링됐고 인증 다운로드는 각각 114,372바이트와 11,853바이트, ZIP 시그니처 `PK\u0003\u0004`를 확인했다. 비인증은 401, 잘못된 경로는 400이었다.
- 전체 테스트 52개, 대상 ESLint, `--check`, 프로덕션 빌드와 `git diff --check`가 통과했다.
- 운영 배포와 운영 사용자 객체·Page 갱신은 배포 전 사용자 확인 조건 때문에 보류했다.
- 사용자 승인 후 `main`의 `c55c17d`를 Vercel 프로덕션에 배포했고 상태 `Ready`를 확인했다.
- 운영 Page `ce39e7b3-b6cd-47c3-80fc-8ec6aa159582`는 지정 원문 링크 1개와 API 첨부 링크 2개를 가지며 기존 공개 원문 ID는 0개다.
- 운영 사용자 비공개 객체는 각각 114,372바이트와 11,853바이트이며 둘 다 ZIP 시그니처 `504b0304`를 확인했다.

## 현재 운영 Stars 미번역 설명 추가 메모

- 현재 운영 `github_stars`에서 설명이 있고 한글이 없는 항목은 10개다.
- 대상은 `NVIDIA/elements`, `nowork-studio/notfair-plugin`, `pranshuparmar/witr`, `ai-for-developers/awesome-ai-coding-tools`, `SmythOS/smythos-studio`, `vectorize-io/hindsight`, `khoj-ai/khoj`, `gooseworks-ai/goose-skills`, `elizaOS/eliza`, `gmickel/flow-next`다.
- 기존 `src/data/star-descriptions-ko.json`, `src/lib/star-translation.ts`, `scripts/add-korean-star-descriptions.mjs`와 `tests/star-translation.test.mjs`를 재사용한다.
- 번역 매핑을 추가한 뒤 운영 DB 백필 스크립트를 실행하고, 영문 전용 설명 0개를 재조회한다.
- 정적 번역표는 207개에서 216개로 늘었고, 기존 `pranshuparmar/witr` 번역은 최신 설명에 맞게 갱신했다.
- 백필 스크립트가 운영 DB 10건을 갱신했고, 대상 10건의 DB 설명 끝부분이 정적 번역과 일치한다.
- 운영 `github_stars` 설명 423건을 재조회해 한글 없는 설명 0건을 확인했다.
- 대상 테스트와 전체 테스트 53개, 변경 테스트 ESLint, 일회성 `AUTH_SECRET` 주입 프로덕션 빌드가 통과했다. 전체 린트는 기존 변경 범위 밖 오류 23개로 실패했다.
## 2026년 8월 둘째 주 Notion Pages 미디어 이관 메모

- 이번 주는 서울 시간 기준 2026-08-10 00:00부터 현재 시각까지다.
- 추가한 페이지는 연결 사용자 `GILWON YOO`가 직접 생성한 페이지의 생성 시각으로 판정한다.
- Notion 검색 API는 날짜만으로 전체 목록을 열거하지 못하므로 여러 넓은 검색어의 합집합과 최근 페이지 화면을 대조했다.
- 확인된 후보는 15건이다. 기존 Pages에서 원문 식별자가 같은 항목은 `김효율 스킬팩` 1건이며 ZIP 2개가 이미 정상이라 스킵한다.
- 나머지 14건은 신규 이관 대상이다. 원문 미디어는 이미지 11개와 `moodmode-insta-saver.zip` 1개다.
- 중복은 원문 식별자를 우선하고 정규화한 정확한 제목을 보조로 사용한다. 제목 또는 원문 후보가 여러 건이면 쓰기 전에 중단한다.
- 기존 중복의 원문 미디어 개수보다 저장된 미디어가 적으면 본문과 미디어를 보완하고, 이미 완비됐으면 수정하지 않는다.
- 만료되는 Notion 서명 URL은 저장하지 않는다. 이미지는 영구 자산으로, ZIP은 인증된 사용자만 받는 기존 `page-attachments` Storage 흐름으로 보존한다.
- 프로덕션 배포와 운영 DB·Storage 변경은 사용자 승인을 받은 뒤 실행한다.
- 운영 배포 `dpl_DNLCsxeqR5TGQnyev8Z1BwA1Ud5R`가 Ready 상태이며 기본 주소 `https://bookmark-ivory-xi.vercel.app`에 연결됐다.
- 첫 이관은 운영 Pages 361건의 큰 본문을 1,000건 단위로 조회해 Supabase `57014 statement timeout`으로 중단됐다. 로컬 14건과 ZIP 2개가 완료되고 운영 Page는 0건인 상태를 확인했다.
- 조회 단위를 100건으로 줄인 뒤 운영 Page 14건을 신규 저장했다. 재실행은 로컬·운영 모두 신규·갱신 0건, 스킵 14건이었다.
- 운영 화면에서 네 Page의 이미지 `2 + 3 + 5 + 1 = 11개`가 모두 표시됐고 ZIP은 10,279,989바이트·시그니처 `504b0304`로 다운로드됐다.
- 전체 테스트 66개, 대상 ESLint, 프로덕션 빌드, Spec·Standards 리뷰가 통과했다.
## CLAUDE.md Notion Page와 첨부 이관 메모

- 원문은 `https://past-teacher-021.notion.site/CLAUDE-md-3bb3de874c4d80189cf4f2c0599b9296`다.
- 기존 `page-attachments` 비공개 Storage와 로그인 소유자 다운로드 경로를 재사용한다.
- 제목·원문 후보가 모호하면 쓰기 전에 중단하고, 완비 중복은 수정하지 않는다.
- 완료 기준은 로컬·운영에 동일 Page가 각 1건 존재하고 모든 첨부를 실제 다운로드하며 재실행 쓰기 0건인 것이다.

## 공개 Notion 문서 5건 Pages 이관 메모

- 대상은 `상세페이지 자동 분석 & 구성 템플릿 활용법 메뉴얼`, `내 분야에서 유명해지는 10가지 GPT 프롬프트`, `(공유) 카페 포스터 스타일 3종`, `일잘알들이 쓰는 GPT 스킬 100개`, `클로드 명령어 100개 + 보너스 33개`다.
- 원문 query의 `fbclid`와 `source`는 저장하지 않고 공개 Notion 정규 URL만 보존한다.
- 공개 `loadPageChunk`를 cursor가 끝날 때까지 조회한 원문 블록 수는 순서대로 75·154·63·5·186개다.
- 본문 이미지 블록은 순서대로 1·5·10·0·0개다. 둘째 문서의 이미지 5개 중 4개는 원문이 가진 1×1 투명 GIF지만 이미지 블록 누락을 피하려 그대로 보존한다.
- 넷째 문서의 Notion 표지 이미지 1개도 원문에서 보이는 이미지로 간주해 본문 첫 이미지로 보존한다. 페이지 아이콘과 소셜 미리보기 메타 이미지는 본문 자산이 아니므로 제외한다.
- 넷째 문서의 `GPT업무명령어100_v1_20260813.pdf`는 공개 Google Drive 파일이며 직접 다운로드 URL이 149,489바이트 PDF와 `Content-Disposition: attachment`를 반환한다. 별도 Storage 경로를 추가하지 않고 이 URL을 사용한다.
- 첫 문서의 표는 `table_block_column_order`와 `table_row.properties` 형식이므로 기존 자식 셀 전용 변환을 그대로 쓰면 셀이 사라진다.
- 로컬·운영 Pages를 제목과 원문 ID로 조회한 결과 다섯 문서 모두 기존 행이 없다. 구현은 그래도 중복이면 완비 문서를 스킵하고 이미지나 첨부가 빠진 정확한 동일 문서만 보완해야 한다.
- 완료 기준은 로컬·운영에 Page 5건이 각각 한 건씩 존재하고 TipTap 이미지 17개와 PDF 직접 다운로드 링크 1개가 보존되며 재실행 쓰기 0건, 실제 이미지 표시와 PDF 다운로드, 전체 테스트·대상 린트·빌드 통과를 확인하는 것이다.
- 로컬·운영에 5건씩 저장됐고 운영 화면에서 이미지 17개의 로딩과 PDF 직접 다운로드 링크를 확인했다. PDF는 149,489바이트이며 SHA-256은 `40207b46eedc9cd775843f0dd868d5d59da7ade2c2f57d94a359fd9f43f92d2e`다.
- 대용량 본문을 `content ILIKE`로 전수 조회하는 운영 후검증이 Supabase `57014 statement timeout`으로 같은 원인 3회 실패했다. 이후 조회를 제목 메타데이터와 정확한 후보 ID 본문으로 제한했지만 멈춤 조건에 따라 운영 재실행은 하지 않았다.
- 최종 코드 기준 전체 테스트 82개, 대상 ESLint·구문 검사·프로덕션 빌드·diff 검사가 통과했다. 최종 읽기 전용 `--check`에서 로컬·운영 모두 `skip` 5건과 `writes: 0`을 확인했다.

## 2026년 8월 셋째 주 Notion Pages 추가 이관 메모

- 현재 날짜는 2026-08-15이며 이번 주 범위는 한국 시간 2026-08-10 00:00부터 2026-08-16 00:00 직전이다.
- 연결된 Notion 사용자는 `GILWON YOO`이며 사용자 ID는 `e2e9c316-1a63-4bcf-9b4f-0e2683293d2dc`다.
- 기존 `scripts/import-notion-week-20260810.mjs`가 처리하는 14건과 별도로 8월 13~15일 후보 8건을 확인했다.
- 후보는 제목 또는 본문 원문 식별자가 기존 Pages에 있으면 스킵한다.
- 이미지에는 만료되는 Notion 서명 URL을 저장하지 않고 data URL 또는 영구 Storage 경로를 사용한다.
- 첨부 파일은 기존 비공개 `page-attachments` Storage와 인증 다운로드 route를 재사용한다.
- 실제 후보 8건 중 `업무시간 단축시켜주는 ChatGPT 프롬프 40가지`는 기존 Page라 스킵했고, 신규 7건을 로컬·운영에 각각 저장했다.
- 신규 7건의 이미지 40개는 TipTap `image` 노드의 data URL로 저장됐고, 저장 후 로컬·운영 본문에서 이미지 40개와 안전하지 않은 서명 URL 0개를 확인했다.
- 이번 후보 8건의 Notion 원문에는 `<file>` 첨부 블록이 없어 첨부 링크는 0건이었다. 기존 Pages 첨부 다운로드 route와 클릭 가능한 링크 렌더링은 그대로 재사용한다.
- 검증 결과 전체 테스트 82개, 변경 스크립트 ESLint, 구문 검사가 통과했다. 프로덕션 빌드는 기존 실행 중인 Next 빌드 잠금 때문에 새 실행이 거절됐다.

## 2026-08-15~16 KST Notion 최신 Pages 이관 메모

- 한국 시간 기준 범위는 2026-08-15 00:00부터 2026-08-17 00:00 미만으로 둔다.
- 이번 요청은 Pages만 대상으로 하며 Prompts 추가는 범위에서 제외한다.
- 후보 검색, 기존 Pages 중복 대조, 본문 이미지·첨부 보존 결과를 아래에 기록한다.
- Notion 날짜 검색은 2026-08-15 검색에서 일반 콘텐츠 3건을 확인했고 2026-08-16 검색은 0건이었다. 연결 사용자 ID 필터는 MCP가 반환한 비표준 UUID로 거절되어 날짜 검색 후 페이지 유형과 제목을 수동 대조했다.
- 이관 후보는 `🌐 10년차 디자이너가 되는 레퍼런스 사이트 20가지 총정리!`, `클로드에 꼭 설치해야 할 24가지 (플러그인·스킬·MCP)`, `클로드코드 구조 제대로 파악하기`다. 자동 일지와 데이터베이스 결과는 콘텐츠 Pages 범위에서 제외했다.
- 후보 원문 미디어는 이미지 20·1·0개, 첨부파일 0·0·0개다. 로컬 Pages에는 앞의 두 문서가 이미 있어 중복 스킵 대상이고 세 번째만 신규 후보였다.
- 실제 이관은 로컬·운영에 `클로드코드 구조 제대로 파악하기` 1건씩 추가했고, 기존 2건은 각각 스킵했다. 세 문서의 TipTap 이미지 수는 양쪽 모두 20·1·0개이며 만료 서명 URL은 0개다.
- 첨부파일은 후보 3건 모두 0개였다. 재실행 결과 로컬·운영 모두 inserted 0, skipped 3이었다.

## 2026-08-16~17 KST Notion Pages 이관 메모

- 기준 시각은 2026-08-17 12:48 KST다. 범위는 2026-08-16 00:00 KST부터 현재까지다. UTC로는 2026-08-15T15:00:00Z 이후 생성분이다.
- Notion MCP `created_date_range`는 달력 날짜라 UTC 8월 15일 생성분도 함께 본 뒤 KST로 다시 걸렀다.
- 연결 워크스페이스는 `GILWON`, 작성자 필터는 `GILWON YOO`다.
- 어제·오늘 생성 페이지는 3건이다.
- `클로드코드 구조 제대로 파악하기`(2026-08-15T22:19:00Z, 16일 07:19 KST)는 이미 Pages에 있어 스킵한다.
- 신규는 `Claude를 천재로 만드는 10가지 기능 & 프롬프트`(2026-08-16T03:06:00Z, 16일 12:06 KST)와 `클로드 코드 플러그인 5개 설치 가이드`(2026-08-16T22:09:00Z, 17일 07:09 KST)다.
- 8월 17일 UTC 생성분은 검색 0건이다.
- 천재 페이지의 잘린 드라이브 블록은 구글 문서 URL이고, 북마크는 네이버 카페 카드인데 공개 대상 URL은 없다. 카페 URL을 지어내지 않는다.
- 플러그인 페이지는 ZIP을 MCP가 주지 않았다. 본문에 있는 GitHub 원문 링크를 유지하고 가짜 첨부를 만들지 않는다.
- Pages만 추가하고 Prompts는 넣지 않는다.
- 로컬·운영 각각 신규 2건을 넣었고 재실행은 양쪽 모두 inserted 0, skipped 2였다.
- 플러그인 페이지 ZIP은 MCP가 주지 않아 GitHub 원문 링크만 다운로드 칸에 넣었다.

## Naver Cafe 546 Pages 이관 메모

- 원문은 `https://cafe.naver.com/hongarin/546`이며 공개 Cafe API에서 제목·본문 HTML을 확인한다.
- 원문 제목은 `블로그 글 쓰는 AI 팀 만들기 전 과정 공개합니다 (클로드 코워크 + n8n 자동 발행)`이다.
- 원문에는 이미지 1개와 첨부파일 0개가 있다.
- 네이버 썸네일 경유 이미지 URL은 다운로드 후 data URL로 Pages 본문에 보존한다.
- Pages만 추가하며 Prompts에는 추가하지 않는다.

- 추가 링크 10건을 포함해 총 11건을 확인했고, 546·481·468·451·143·192·215·300번은 공개 원문을 확보했다.
- 공개 8건을 로컬·운영에 각각 저장했으며, 재실행은 모두 `skipped`였다.
- 공개 원문 이미지 총 2개는 양쪽 본문에 `data:image`로 저장됐고 첨부파일은 모두 0개였다.
- 243·252·262번은 네이버 API와 브라우저 모두 로그인 필요 상태라 본문을 확보하지 못해 저장하지 않았다.
- 대상 이관 스크립트 린트, 전체 테스트 88개, 프로덕션 빌드가 통과했다.

## Naver Cafe 로그인 세션 보완 메모

- `aside-browser` 로그인 세션에서 243·252·262번 API 원문을 다시 확인했다.
- 243번 제목은 `왕초보도 가능! AI 과일 드라마 쇼츠 제작법 전 과정 공개합니다 (제미나이 + 힉스필드 + 구글비즈)`, 252번은 `NotebookLM으로 유튜브 주제부터 영상까지 만드는 법 (완전 무료) | 콘텐츠 제작 시스템`, 262번은 `GPT 이미지 2로 상세페이지 만들기 — 실습 프롬프트 전체 정리`다.
- 세 원문 모두 첨부파일은 0개이며, 262번에만 이미지 7개가 있다.
- 3건을 로컬·운영에 각각 저장했고 재실행은 모두 중복 스킵이었다. 양쪽 본문에서 262번 이미지 7개가 모두 data URL임을 확인했다.
- 전체 11건이 로컬·운영에 각각 존재하며, 대상 린트·전체 테스트 88개·프로덕션 빌드가 통과했다.

## SurgeDesk 공개 문서 Pages 이관 메모

- 대상은 `https://surgedesk.co.kr/quant-runbook`, `https://surgedesk.co.kr/prompts#toss-slack-vcp`, `https://surgedesk.co.kr/prompts#claude-tradingview`다.
- `prompts` 두 URL은 공통 페이지의 해당 앵커 섹션만 별도 Page로 저장한다.
- Quant 런북은 정적 HTML 본문을 변환했고 전체 구조도 SVG를 data URL 이미지 1개로 보존했다. 별도 첨부파일은 없다.
- `toss-slack-vcp`와 `claude-tradingview`는 공통 JavaScript 데이터에서 각각 해당 slug의 본문만 추출했다. Toss 문서의 ZIP은 공개 직접 다운로드 링크를 유지했고 실제 GET으로 109,724바이트 ZIP을 확인했다.
- 세 문서를 로컬·운영에 각각 저장했고, 재실행은 모두 중복 스킵이었다.
- 저장 후 양쪽에 문서 3건, Quant 이미지 1개, Toss ZIP 링크 1개가 존재함을 재조회했다.
- 대상 이관 스크립트 린트, 전체 테스트 88개, 프로덕션 빌드가 통과했다.

## 네이버 사이트 퍼널 자료 26건 이관 메모

- 짧은 링크는 모두 308로 공개 Notion으로 넘어간다. 24·26번만 `notion-gate-murex.vercel.app` 미리보기를 거쳐 같은 워크스페이스 Notion으로 연결된다.
- 워크스페이스는 `surf-push-7ae.notion.site`다.
- 이미 있는 Pages는 이모지 접두만 다르고 정규화 제목이 같다. 1~6번을 스킵한다.
- 스킵 대상은 고객이 매일 쌓이는 자동화 프롬프트 30개, 수익 구간별 프롬프트, 매출 5배 AI 프롬프트 20가지, 경쟁사 광고 복제, AI 퍼널 실전 가이드, 수익화 아이템 발굴 프롬프트다.
- 기존 사본은 `app.notion.com/p/...` 주소라 원문 ID로는 잡을 수 없다. 제목 정규화만 쓴다.
- 이미지는 서명 URL을 그대로 두지 않고 data URL로 넣는다.
- 코드 블록만 Prompts로 분리한다. 설치 명령은 넣지 않는다.
- 24·26번은 리드 폼을 제출하지 않고 공개 미리보기 Notion만 저장한다.
- 신규 20건을 로컬 18+운영 20으로 저장했다. 7·8번은 로컬에 먼저 들어가 로컬은 스킵, 운영만 추가했다.
- 원문 이미지 블록은 신규 20건에 없었다. 서명 URL도 본문에 없다.
- 코드 블록 프롬프트는 로컬 76+11, 운영 79+11이다. 25번은 첫 청크에서 잘려 커서 페이지네이션 후 30개로 보정했다.
- 운영 본문 `LIKE` 검색은 statement timeout이 나서 제목 정규화만 쓴다.
- 재실행 `--check`는 로컬·운영 26건 모두 스킵이다.

## Mac 터미널 기본 설정 가이드 이관 메모

- 원문은 영상 대본이다. 사용자 요청대로 `00:28` 같은 분·초 표시는 본문에서 뺀다.
- 터미널 명령은 AI 프롬프트가 아니지만 복사해 다시 쓰는 블록이라 Prompts로 분리한다.
- 스크린샷 경로는 원문 그대로 `/Users/kst/Desktop/screenshots`를 둔다.
- `killall Finder` 주의문은 본문 앞에 그대로 둔다.

## 블로그 치트키 제목 400개 이관 메모

- 원문은 `https://halved-stretch-7af.notion.site/400-3bf9770df064809ebd67ec2047571b45`다.
- 제목은 `조회수 터지는 블로그 치트키 제목 400개`다.
- 표는 결과 양식 헤더 1행만 있고, 400개 제목은 프롬프트가 만들도록 되어 있다.
- 이미지 3개는 1×1 GIF 자리 표시다. 원문과 같이 data URL로 둔다.
- 콜아웃 안의 지시문 전체가 복사용 프롬프트 1개다.
- 로컬·운영에 Page 1건과 Prompt 1건을 저장했고 재실행은 스킵이다.

## 짐코딩 Matt Pocock 스킬 설치 가이드 Pages 이관 메모

- 사용자 URL은 `fbclid`가 붙어 있다. 저장 원문은 `https://www.gymcoding.co/articles/matt-pocock-ai-skills-install-guide`다.
- 제목은 `Matt Pocock 스킬 설치와 활용법: 요즘 AI 잘 쓰는 사람은 프롬프트를 매번 쓰지 않습니다`다.
- 본문은 `<article class="py-page-y min-w-0">`다. 로고 SVG, 인프런 강의 카드, 짐코딩 뉴스레터 폼은 뺀다.
- 표지 OG PNG는 만료 쿼리가 있어 data URL로 고정한다.
- 설치 명령과 `/grill-me` 등 실습 프롬프트는 코드 펜스에 원문 그대로 둔다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목의 새 행은 만들지 않는다. 로컬은 본문이 같으면 스킵하고 다르면 UPDATE한다. 운영은 제목이 있고 FAQ 답이 있으면 스킵한다. TipTap 자동저장이 JSON을 바꿔 전체 본문 비교가 매번 UPDATE가 되기 때문이다. FAQ가 빠진 기존 행만 UPDATE한다.
- 정적 HTML의 FAQ `role=region`은 닫혀 있어 답이 비어 있다. 브라우저에서 질문별로 연 뒤 `scripts/import-gymcoding-matt-pocock-faq.json` 9개를 `fillFaqAnswers`로 넣는다.
- 기존 `클로드 코드 스킬·도구 추천 10개`는 다른 gymcoding 글이라 중복이 아니다.
- 저장 id는 `2012d60f-87b0-48a2-80f9-2238dcca2c0b`다. 로컬 `dev`와 운영 사용자에 같다. 표지 PNG 1장, 코드 블록 17개다. Prompts 행은 만들지 않았다.

## 짐코딩 클로드 스킬 10개 이관 메모

- 원문은 `https://www.gymcoding.co/articles/claude-code-skills-top-10-install-prompts`다.
- 정적 HTML에는 아코디언 본문이 비어 있어 브라우저에서 토글을 연 뒤 추출했다.
- FAQ는 한 번에 하나만 열려 질문별로 따로 열어 답을 받았다.
- 인프런 강의 카드와 뉴스레터 폼은 본문에서 뺀다.
- 기존 `핫한 깃허브 클로드 스킬 TOP 10`은 다른 Notion 원문이라 중복이 아니다.

## 워프센스 AI 에이전트 팀 이관 메모

- 원문은 `https://worpsense.com/클로드로-나만의-ai-에이전트-팀-만들기｜실제-프롬프트/`다.
- 제목은 `클로드로 나만의 AI 에이전트 팀 만들기｜실제 프롬프트와 설정 방법`이다.
- 복사용 본문은 planner·coder·tester·reviewer·ship 스킬과 `/ship` 실행 예시다.
- 뉴스레터·관련글·멀린 배너는 본문에서 뺀다.

## 유튜브 쇼츠 프롬프트 추가 메모

- 사용자가 전달한 프롬프트는 쇼츠 주제 발굴, 45초 대본, 훅, 지속률 재구성, 30일 아이디어, 롱폼 분할, 경쟁 채널 분석, 수익화까지 8개다.
- Page 원문은 없으므로 Prompts만 추가한다.
- 카테고리는 `유튜브 쇼츠`로 통일한다.
- 프롬프트 본문은 사용자가 준 문구를 임의로 요약하거나 재작성하지 않고 sections의 `프롬프트`에 그대로 저장한다.
- 중복은 사용자별 제목·카테고리 또는 동일 프롬프트 본문으로 판정한다.

## 클로드 코드 /design Notion Page 이관 메모

- 원문은 `https://app.notion.com/p/design-3c0fd99f0e5f8102ad3ed2294e75c755`이며 제목은 `클로드 코드 /design 시작 가이드`다.
- Notion API 연결에는 404였지만 로그인된 브라우저에서 원문을 확인했고, 본문·표·코드 예시·공식 링크·이미지 9개를 추출했다.
- 앱 Pages에는 Notion 원문 링크를 포함한 TipTap 본문과 이미지 data URL 9개를 로컬·운영에 각각 저장했다.
- 재실행 중복 검사는 로컬·운영 모두 `true`였고, 이관 스크립트 ESLint 오류·경고는 없다.

## 더마르콩 상세페이지 전략 Page 이관 메모

- 사용자가 붙여넣은 5단계 상세페이지 설득 시나리오를 Page로 저장한다.
- 제목은 중복이 없는 `월 1억 브랜드 상세페이지 설득 시나리오`로 정한다.
- 기존 운영 본문 `ilike` 검색은 statement timeout이어서 제목 목록을 읽어 중복을 확인했고, 동일 제목은 없었다.
- `월 1억 브랜드 상세페이지 설득 시나리오`를 로컬·운영에 각각 저장했고, 5단계 제목과 `100% 전액 환불 보장` 문구를 재조회로 확인했다.
- 재실행 시 로컬·운영 모두 중복 스킵 상태다.

## GitHub Stars 중복 레포 정리 메모

- 운영 `github_stars`는 전체 1,909행이며 모두 `source=sync`다.
- 현재 행은 이전 로그인 세션의 사용자 ID 8개에 저장되어 있고, 사용자별 중복은 없지만 287개 레포가 세션 간 반복된다.
- 안정 사용자 ID `f72e9a44-79d8-4061-a700-3ec50bb04a97`에 이미 287개 전체 레포가 있으므로 이 데이터를 보존하고 다른 7개 사용자 ID의 1,622행을 삭제한다.
- 실제 삭제는 안정 사용자 ID를 제외한 `source=sync` 1,622행에만 적용했고, 안정 사용자 ID의 287행은 보존했다.

## 30초 AI 요리 릴스 Notion Page 이관 메모

- 원문은 `https://pineapple-pyroraptor-9c5.notion.site/30-AI-3bf72839155881f38eafd425391cdacf`다.
- 페이지 ID는 `3bf72839-1558-81f3-8eaf-d425391cdacf`이고 제목은 `캐릭터로 만드는 30초 AI 요리 릴스 범용 프롬프트`다.
- Notion MCP 연결 워크스페이스에서는 404지만 공개 `loadPageChunk` API로 읽을 수 있다.
- 조사 시점 루트 본문 블록은 22개다. 코드 블록 3개, 이미지 블록 0개, 파일·PDF 블록 0개다.
- 이전 이관과 같이 이미지 블록이 있으면 서명 URL을 그대로 두지 않고 data URL로 고정한다. 파일 블록이 있으면 본문에서 빠지지 않게 저장한다.
- 코드 블록 3개 중 완성형 프롬프트 1개를 Prompts로 분리하고, RECIPE STYLE 두 줄은 Page 본문에만 둔다.
- Prompt 카테고리는 `Notion · 캐릭터로 만드는 30초 AI 요리 릴스 범용 프롬프트`로 한다.
- 기존 `scripts/import-blog-title-400.mjs`의 공개 블록 로드·Markdown→TipTap 저장과 `scripts/import-notion-link-batch-20260815.mjs`의 이미지·첨부 처리 패턴을 재사용한다.
- 로컬 사용자 `dev`, 운영 사용자 `f72e9a44-79d8-4061-a700-3ec50bb04a97`에 저장하고 같은 제목·원문 ID는 수정하지 않고 스킵한다.
- 실행 결과 라이브 이미지 0개, 첨부 0개, 코드 블록 3개, 완성형 프롬프트 8015자다. 변환 파이프라인은 유지했다.
- 연속 목록을 빈 줄로 나누면 번호가 전부 1로 보여 `--refresh`로 사용 방법 3항목·핵심 보완 5항목을 한 목록으로 고쳤다.
- 로컬 Page `2662a909-e63b-4757-84d3-f87d04ce1b04`, Prompt `1cb1456e-2fc6-4ca4-a41e-603f1f2543e5`다.
- 운영 Page `5e2f3c63-ed4e-4ee9-9e41-b47cf2927072`, Prompt `2fca1b79-4cbf-4683-92f8-b2e198f229bc`다.
- 재실행은 로컬·운영 모두 skip이다. 대상 스크립트 ESLint는 통과했다.

## 한국 시간 8월 17일 이후 Notion 이관 메모

- 범위는 서울 시간 2026-08-17 00:00 이후다. UTC로는 `2026-08-16T15:00:00.000Z`부터다.
- 연결 워크스페이스는 `GILWON`, 생성자 `GILWON YOO` (`e2e9c316-1a63-4bcf-9b4f-0e268329d2dc`)로 제한한다.
- 검색 API는 날짜만으로 전체 목록을 주지 않아 여러 검색어 합집합을 쓴다.
- 비공개 페이지는 공개 `loadPageChunk`가 비어 있어 MCP `fetch`로 읽고, 이미지 서명 URL은 5분 만료라 즉시 data URL로 고정한다.
- 후보 9건은 Gmail Dashboard, Design.md 뽀개기, 재질 표현, SpaceXAI 스택, 침대 원격 설정, 마케팅 올인원, 구글 코딩 스킬 24, 펀드매니저, 스태터스 라인이다.
- 공개 사이트 `캐릭터로 만드는 30초 AI 요리 릴스 범용 프롬프트`는 이미 저장돼 스킵한다.
- 프롬프트는 펜스 코드 중 재사용 본문과 재질 표현 9개를 저장한다. `npx`·`curl`·`git` 한 줄과 mermaid는 Prompts에서 뺀다.
- 같은 제목 또는 원문 ID가 본문에 있으면 수정하지 않고 스킵한다.
- 비공개 페이지 9건을 MCP로 읽고 이미지 13장을 로컬 파일로 받은 뒤 data URL로 넣었다. ZIP 첨부는 없었다.
- Prompts는 Gmail 1, Design.md 1, 재질 9, 마케팅 6, 스태터스 라인 1로 합계 18개다. npx·curl·git·mermaid는 넣지 않았다.
- 로컬·운영 첫 실행은 Page 9·Prompt 18 신규였고, 재실행은 모두 스킵이다. 요리 릴스 페이지는 그대로 1건이다.

## 한국 시간 8월 19일 이후 Notion 이관 메모

- 범위는 서울 시간 2026-08-19 00:00 이후다. UTC로는 `2026-08-18T15:00:00.000Z`부터다. 오늘(2026-08-21)까지의 생성분을 본다.
- 연결 워크스페이스는 `GILWON`, 생성자 `GILWON YOO` (`e2e9c316-1a63-4bcf-9b4f-0e268329d2dc`)로 제한한다.
- 검색 API는 날짜만으로 전체 목록을 주지 않아 여러 검색어 합집합을 쓴다. `created_date_range`는 달력 날짜라 UTC 8월 18일 생성분도 함께 본 뒤 KST로 다시 건다.
- 같은 제목 또는 원문 ID가 본문에 있으면 수정하지 않고 스킵한다.
- 이미지는 5분 만료 서명 URL을 저장하지 않고 data URL로 고정한다.
- 첨부파일은 기존 `page-attachments` Storage와 인증 다운로드 route를 재사용하거나, 공개 직접 다운로드 URL이 있으면 그 URL을 쓴다.
- 프롬프트는 펜스 코드 중 재사용 본문을 Prompts로 분리한다. `npx`·`curl`·`git` 한 줄과 mermaid는 제외한다.
- 8월 17일 이후 이관분(Gmail Dashboard, Design.md 뽀개기 등 9건)은 이미 있으므로 중복 스킵 대상이다.
- 검색 합집합에서 콘텐츠 페이지는 7건이다. Gmail Dashboard, 재질 표현, SpaceXAI, 침대 원격 설정, 구글 코딩 스킬 24, 펀드매니저, 스태터스 라인이다. 모두 제목·원문 ID가 이미 Pages에 있어 신규 쓰기는 0건이다.
- Design.md 뽀개기는 2026-08-18 17:41 KST라 이번 범위 밖이다. 시장조사 올인원은 2026-08-17 20:58 KST라 범위 밖이다. 둘 다 기존 이관분이다.
- 2026-08-20·21 검색의 신규 행은 `특별히 성장하는 날`, `@오늘 오전 12:00` 같은 노시언 일기·감정 평점 템플릿뿐이다. 자동 일지와 데이터베이스 결과는 콘텐츠 Pages에서 뺀다.
- 8월 20일 UTC 이후 신규 콘텐츠 페이지는 0건이다.
- 후보 7건의 ZIP·파일 첨부 블록은 0개다. 이미지는 로컬·운영 모두 data URL이며 만료 서명 URL은 없다. 이미지 수는 2·2·3·1·0·0·0개다.
- Prompts는 Gmail 1, 재질 9, 스태터스 라인 1이다. SpaceXAI·침대·구글 스킬 24·펀드매니저는 재사용 펜스 본문이 없거나 CLI·mermaid라 0개다.
- 운영 화면에서 Gmail·재질·침대·SpaceXAI 이미지 2·2·1·3개가 모두 `naturalWidth`를 갖고 깨지지 않았다. 프롬프트 목록의 Notion 카테고리는 608건이다.

## GitHub Star 영문 전용 설명 한국어 병기 메모

- 운영 `github_stars` 297건 중 설명이 비어 있는 21건은 번역 대상이 아니다. 한글이 없는 영문 설명은 29건이다.
- 기존 정적 매핑에 있는 저장소는 0건이다. `MarsWang42/OrbitOS`는 이미 번역돼 있고, 영문 전용은 `MarsZ42/OrbitOS`라 별도 키로 넣는다.
- 표시 형식은 영문 원문 다음 빈 줄 뒤 한국어다. 동기화 시 `withKoreanTranslation`이 매핑을 병기한다.
- 설명 없는 Star는 원문이 없어 한국어만 지어 넣지 않는다.

## ChatGPT 블로그 조회수부터 수익화까지 Notion 이관 메모

- 원문 URL은 `https://halved-stretch-7af.notion.site/ChatGPT-3c29770df064808a9dc0f03edf9834f3`다.
- 페이지 ID는 `3c29770d-f064-808a-9dc0-f03edf9834f3`이고 제목은 `ChatGPT로 블로그 조회수부터 수익화까지 🚀`다.
- 연결된 Notion MCP는 404다. 공개 `loadPageChunk`는 읽을 수 있다. `limit: 100`이면 루트 하위가 잘리므로 커서로 이어 받아야 한다.
- 블록은 159개(페이지 1 + 하위 158)이고 루트 하위는 157개다. 이미지·표·코드 블록은 없다. 토글도 없다.
- 종류는 text 79, bulleted_list 33, sub_sub_header 15, divider 10, header 9, numbered_list 7, header_4 2, sub_header 2, quote 1이다.
- 인용 블록 1개는 자식 텍스트가 있다. `[분야]`, `[주제]`를 바꿔 붙이라는 사용법이다.
- 링크 8개는 `a` 마크의 href를 쓴다. 표시 텍스트와 href가 다른 경우가 있다. 카카오 오픈채팅 2번방은 텍스트 `https://open.kakao.com/o/g87MMj`이고 href는 `https://open.kakao.com/o/g87MMjIg`다. 스레드 한 건은 href에 `?hl=ko`가 있다.
- 원문에 `*[주제]**에 대한 블로그 글을 작성해주세요.`처럼 볼드 마크다운이 깨진 문자열이 있다. 원문 그대로 둔다. 고치면 원문과 달라진다.
- 빈 텍스트 블록 5개는 저장 본문에서 뺀다. 보이지 않는 공백만 늘리기 때문이다.
- 페이지 안 Notion `header`를 마크다운 `#`로 두면 h1이 10개가 된다. 페이지 제목만 `#`로 두고 `header`는 `##`, `sub_header`는 `###`, `sub_sub_header`는 `####`, `header_4`는 `#####`로 둔다.
- 기존 Page `ChatGPT로 네이버 블로그 주제 찾고 글까지 만드는 방법`은 다른 원문이다. 제목과 ID가 달라 스킵 대상이 아니다.
- 사용자는 Pages만 요청했다. 복붙 프롬프트 7개는 코드 펜스가 아니라 문단·목록이라 Prompts로 나누지 않는다. 페이지 본문에 그대로 둔다.
- 같은 제목 또는 본문의 원문 ID가 있으면 수정하지 않고 스킵한다.
- 기존 `scripts/import-notion-omc-guide.mjs`의 공개 블록 렌더와 Markdown→TipTap 저장을 재사용하고 새 의존성은 추가하지 않는다.
- 링크 조각에 볼드 마크가 있으면 `[**url**](href)`가 되어 링크 문구에 별표가 남는다. 링크가 있으면 볼드 마크다운은 씌우지 않는다.
- 원문 `*[주제]**`는 TipTap 이탤릭 파서가 `[주제]*`로 삼킨다. 그 줄만 인라인 코드로 감싼다. `**[주제]**`는 볼드로 둔다.
- 운영 `custom_pages.content LIKE` 전체 스캔은 statement timeout이 난다. 중복은 제목 조회로만 본다.
- 저장 id는 `3ea15d9b-09a6-4756-affc-f9a36399622d`다. 로컬 `dev`와 운영 `f72e9a44-79d8-4061-a700-3ec50bb04a97`에 같다.

## 네이버 블로그 클로드 세팅 핵심 프롬프트 Pages 메모

- 원문은 채팅으로 받은 프롬프트 4개다. Notion URL은 없다.
- 제목은 `네이버 블로그를 클로드 세팅법 핵심 프롬프트`다.
- 기존 Page `ChatGPT로 네이버 블로그 주제 찾고 글까지 만드는 방법`은 ChatGPT 원문이라 다른 문서다.
- 사용자는 Pages만 요청했다. 프롬프트 4개는 페이지 본문 코드 펜스로 둔다. Prompts 테이블에는 넣지 않는다.
- 채팅의 `####`는 페이지 제목이 이미 h1이므로 섹션을 `##`로 둔다. 그렇지 않으면 h4만 쌓인다.
- 프롬프트 본문은 펜스 안에 원문 그대로 둔다. `[분야]`·`[주제]`를 바꾸지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `3cafe6b0-afe9-44ea-b250-5a0a1255b572`다. 로컬 `dev`와 운영 사용자에 같다.
- 페이지 목록 검색 `클로드 세팅법`으로 이 문서가 잡힌다. 본문 코드 블록 4개가 에디터에 보인다.

## 클로드 공식 무료 강의 시작 가이드 Notion 이관 메모

- 원문 URL은 `https://app.notion.com/p/3c3fd99f0e5f8170b159cf33eb5f6ee4?source=copy_link`다.
- 페이지 ID는 `3c3fd99f-0e5f-8170-b159-cf33eb5f6ee4`이고 제목은 `🎓 클로드 공식 무료 강의 시작 가이드`다.
- 연결된 Notion MCP는 404다. 공개 `loadPageChunk`로 읽는다.
- 루트 하위는 51개다. 토글 9개의 자식은 별도 요청으로 이어 받는다. `limit: 100` 한 번에 토글 안이 비어 있다.
- 같은 청크에 하위 페이지 `프롬왓 | Prompt What`이 있고 자식이 205개다. 이 가이드의 본문이 아니라서 펼치지 않는다.
- 이미지 블록 5개는 `attachment:`라 서명 URL을 data URL로 고정한다. 커버 `https://academy.claude.com/og/site.png`도 data URL로 둔다.
- 표 1개, 콜아웃 4개, 컬럼 2쌍이 있다. 코드 블록은 없다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `4ac42bce-be2f-440b-81f0-3a62bc7653c1`다. 로컬·운영에 같다. 이미지 6장은 모두 data URL이며 에디터에서 naturalWidth가 있다.

## 2026-08-21 Notion 신규 페이지 이관 메모

- 범위는 서울 시간 2026-08-21 00:00 이후, 생성자 `GILWON YOO`다.
- 콘텐츠 페이지는 3건이다. EP.36 무료 프롬프트 사이트 8개, 코드 없이 시작하는 업무 자동화 가이드, 100만 후킹멘트 모음집이다.
- 후킹멘트 모음집은 같은 시각에 하위 페이지·데이터베이스가 다수 생겼다. 상위 분류 페이지만 넣고 하위 공식 페이지와 DB는 링크로 둔다.
- 업무 자동화 가이드에 시스템 이미지 10장이 있다. 5분 만료 서명 URL을 data URL로 고정한다. 파일 첨부는 없다.
- EP.36과 후킹멘트 모음집은 본문 이미지·ZIP이 없다.
- 이미 있는 Pages(아카데미 가이드, ChatGPT 블로그, 네이버 클로드 세팅)는 스킵한다.
- 사용자는 Pages와 이미지·첨부를 요청했다. Prompts 테이블에는 넣지 않는다. 복붙 지시문은 페이지 코드 블록에 그대로 둔다.
- 저장 id는 EP.36 `370a7332-610f-45f6-824e-d7538febfa3a`, 자동화 `a1ae2b67-ceec-4866-bc60-e2f1ab06216b`, 후킹멘트 `5e1f4187-f0dd-4ab8-85a2-1756fee600a8`다.
- 자동화 가이드 이미지 10장은 모두 data URL이고 에디터에서 `naturalWidth` 1080이다. ZIP·파일 첨부는 0개다.

## GPT IMAGE 2.0 API 투명 배경 가이드 Notion 이관 메모

- 원문 URL은 `https://app.notion.com/p/GPT-IMAGE-2-0-API-3c3fd99f0e5f81e7a70fe48d445fd5cd`다.
- 페이지 ID는 `3c3fd99f-0e5f-81e7-a70f-e48d445fd5cd`이고 제목은 `GPT IMAGE 2.0 API 투명 배경 사용 가이드`다.
- 연결된 Notion MCP는 404다. 공개 `loadPageChunk`로 읽는다. space는 `b97fd99f-0e5f-81d6-b46e-0003ed3b57b7`다.
- 루트 하위는 76개다. 토글 3개의 자식은 별도 요청으로 이어 받는다. `limit: 100` 한 번에 토글 안이 비어 있다.
- 같은 청크에 상위 페이지 `프롬왓 | Prompt What`(`d60fd99f-0e5f-8295-a947-81c6fd0e9948`)이 있고 자식이 많다. 이 가이드의 본문이 아니라서 펼치지 않는다.
- 블록 합계는 부모 페이지 레코드를 포함해 141개다. 본문 쪽 누락 자식은 0개다.
- 이미지 블록 4개는 `attachment:`라 서명 URL을 data URL로 고정한다. 페이지 커버는 없다.
- 표 6개, 콜아웃 5개, 코드 블록 5개다. 코드는 이미지 API Python 예제 1개와 게임 에셋 프롬프트 4개다.
- 토글 제목이 원문에서 이미 볼드라 `**`를 한 번 더 씌우면 `****`가 된다. 이미 볼드면 그대로 둔다.
- 이미지 alt는 caption이 있으면 caption, 없으면 파일명을 쓴다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다. 복붙 프롬프트는 페이지 코드 블록에 그대로 둔다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `a05a80b7-ffcb-484a-bd90-9adf273d69e0`다. 로컬 `dev`와 운영 사용자에 같다.
- 운영 조회를 content ILIKE로 하면 statement timeout이 난다. 운영은 제목으로만 중복을 본다.
- 운영 에디터에서 이미지 4장의 naturalWidth는 1024·1440·1920·3000이다. 표 6개, 코드 예제와 게임 프롬프트가 보인다.

## 경력 정리 프롬프트 Pages 이관 메모

- 원문은 사용자 채팅에 붙여 넣은 5단계 프롬프트다. Notion URL은 없다.
- 제목은 `경력 정리 프롬프트`다. 단계는 업무 꺼내기, 성과로 바꾸기, 강점 뽑기, 구조 잡기, 맞춤 정리하기.
- 프롬프트 본문은 펜스 안에 원문 그대로 둔다. `[여기에 쏟아내기]` 자리를 바꾸지 않는다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 없는 경력을 지어내지 말라는 지시가 원문에 있으므로, 이관 때도 문구를 윤문하지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `fec131b0-1b08-46d7-89b9-9846f577a148`다. 로컬 `dev`와 운영 사용자에 같다.
- 운영 목록 검색 `경력 정리 프롬프트` 결과는 1건이다. 상세에서 코드 블록 5개와 `[여기에 쏟아내기]`가 보인다.

## 블로그 주제 찾기 프롬프트 Pages 이관 메모

- 원문은 사용자 채팅에 붙여 넣은 8단계 프롬프트다. Notion URL은 없다.
- 제목은 `블로그 주제 찾기`다. 기존 페이지 `ChatGPT로 네이버 블로그 주제 찾고 글까지 만드는 방법`과는 다른 문서이므로 수정하지 않는다.
- 프롬프트 본문은 펜스 안에 원문 그대로 둔다. `[관심 분야]`·`[타깃 독자]`·`[주제]`·`[키워드]` 자리를 바꾸지 않는다.
- 사용자는 Pages 추가를 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `b0460eac-0a9a-464c-82e6-1ee3083958f3`다. 로컬 `dev`와 운영 사용자에 같다.
- 운영 목록 검색 `블로그 주제 찾기` 결과는 1건이다. 상세에서 코드 블록 8개와 복사 버튼 8개가 보인다.

## Stars 영문 전용 설명 한국어 병기 메모

- 운영 `github_stars` 313건 중 한글이 없는 설명이 15건이다. 설명 자체가 비어 있는 21건은 이번 범위에서 제외한다.
- 15건 중 `holaboss-ai/holaOS`는 매핑이 있는데 백필이 안 된 상태다. 설명 원문이 매핑 작성 시점과 달라져도 기존 한국어를 병기한다.
- `freestylefly/awesome-gpt-image-2`는 영·중문 혼용이다. 한글이 없어서 이번 대상에 포함한다. 기존 `YouMind-OpenLab/awesome-gpt-image-2`와는 다른 저장소다.
- 번역은 `src/data/star-descriptions-ko.json`에 넣고 `withKoreanTranslation`이 이후 동기화에서도 병기하게 한다.
- 운영 반영은 `scripts/add-korean-star-descriptions.mjs`로 한글이 없는 행만 갱신한다.
- 백필 15건을 갱신했고, 한글이 없는 설명은 0건이다. 설명이 비어 있는 21건은 그대로 둔다.

## Stars 빈 설명 GitHub 확인 후 한국어 추가 메모

- GitHub About 설명이 전부 null이라 README, SKILL.md, CLAUDE.md, 저장소 페이지로 내용을 확인했다.
- `vfxguyai/newvideogenerator`는 파일이 없는 빈 저장소다. 그 사실을 한국어로 적는다.
- GitHub 원문이 없으면 영문을 지어내지 않고 한국어만 저장한다. 이후 동기화에서 GitHub 설명이 비어 있으면 이 한글을 유지한다.
- 기존 `if (!description) return previous`는 매핑이 있어도 빈 칸을 그대로 두므로, 매핑이 있으면 한글로 채우도록 바꿨다.
- 백필 21건을 채웠고, 설명이 비어 있거나 한글이 없는 항목은 0건이다.

## 자율루프 세팅 프롬프트 Pages 이관 메모

- 원문은 사용자 채팅에 붙여 넣은 자율 개발 루프 셋업 지시와 2단계 채우기 안내다.
- 제목은 `자율루프 세팅, 프롬프트`다.
- 통째로 붙여넣는 셋업 본문은 펜스 안에 원문 그대로 둔다. `loop/`·`docs/` 경로와 ④⑤ 순서를 윤문하지 않는다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `b8269885-665e-4b3a-85f4-d6cb20252a93`다. 로컬 `dev`와 운영 사용자에 같다.
- 운영 목록 검색 `자율루프 세팅` 결과는 1건이다. 상세에서 셋업 프롬프트 코드 블록 1개와 2단계 안내가 보인다.

## 워프센스 클로드 업무 자동화 45가지 Pages 이관 메모

- 원문 URL은 `https://worpsense.com/ai-자동화-방법-클로드로-업무-자동화하는-45가지-아이/` 다. 저장할 때는 `fbclid`를 뺀다.
- 제목은 `AI 자동화 방법, 클로드로 업무 자동화하는 45가지 아이디어`다.
- 본문은 `.elementor-widget-theme-post-content` 다. 멀린 배너, 뉴스레터 가입, 관련글 썸네일, gravatar는 뺀다.
- 본문 이미지는 WordPress URL이 만료될 수 있어 data URL로 고정한다.
- 아침 트렌드 스캐너 프롬프트는 페이지 코드 블록에 둔다. Prompts 테이블에는 넣지 않는다.
- FAQ는 본문 일부라 포함한다.
- 같은 제목 또는 원문 URL이 있으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `ced8beed-615b-429a-8f2e-8a98309acac4`다. 로컬 `dev`와 운영 사용자에 같다.
- 히어로는 본문 위젯 밖 featured 이미지라 본문 앞에 붙였다. data URL 이미지 5장, 코드 블록 1개다. 뉴스레터와 merlin은 없다.

## 2026-08-22 이후 Notion 신규 Pages 이관 메모

- 범위는 서울 시간 2026-08-22 00:00 이후다. UTC로는 `2026-08-21T15:00:00.000Z`부터다. 오늘(2026-08-24)까지의 생성분을 본다.
- 연결 워크스페이스는 `GILWON`, 생성자 `GILWON YOO` (`e2e9c316-1a63-4bcf-9b4f-0e268329d2dc`)로 제한한다.
- 검색 API는 날짜만으로 전체 목록을 주지 않아 여러 검색어 합집합을 쓴다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다. 중복은 수정하지 않고 스킵한다.
- 같은 제목, `normalizedNotionWeekTitle`이 같은 제목, 또는 본문에 원문 hex가 있으면 스킵한다.
- 8월 21일 이관분 EP.36, 🎛️ 코드 없이 시작하는 업무 자동화 가이드, 100만 후킹멘트 모음집은 이미 있다.
- `a97b256827ac83bb88ff01e5dc3ba1bf`는 8월 22일 생성 복사본이고 제목이 자동화 가이드와 같아 스킵한다.
- 후킹멘트 하위 템플릿·데이터베이스는 부모 페이지가 이미 있어 펼치지 않는다.
- 신규는 `📊 자산관리 대시보드 6가지, 클로드 코드로 직접 만들기`(`c8db256827ac82b0bf8381dc928fde34`, 이미지 6)와 `클로드 플러그인 7개 10분만에 설치하기`(`a4ab256827ac82f785e1813bd31d0687`, 이미지 3)다.
- 기존 `📊 자동화 대시보드 6개 — 직접 만들기`와 Plugin 50선은 제목이 달라 다른 문서다.
- 이미지는 5분 만료 서명 URL을 저장하지 않고 `tmp/notion-kst-20260824/images`의 PNG를 data URL로 고정한다.
- 로컬 id는 대시보드 `198ecb82-3e56-4aee-a5e0-87932a7f3699`(이미지 6), 플러그인 `61d6489c-ea3d-4c07-9d22-4a16a84c0657`(이미지 3)이다. 운영 id는 `7b016ad6-548b-4d8b-babb-33a36f0f3fe4`, `baa600de-a3cc-4c91-bf51-f419c0437111`이다. 재실행은 로컬·운영 모두 skip 2건이다.

## 시니어 엔지니어 모드 프롬프트 7가지 Pages 이관 메모

- 원문은 사용자가 채팅에 붙여 넣은 프롬프트 7개다.
- 제목은 `시니어 엔지니어 모드 프롬프트 7가지`다.
- 각 프롬프트는 코드 펜스 하나에 원문 그대로 둔다. `[아이디어/서비스]`, `[코드베이스/파일]`, `[에러 메시지/코드/재현 상황]`, `[제품/기능]`, `[코드/컴포넌트]`, `[작업 내용]`, `[만들 컴포넌트]`를 윤문하지 않는다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `9e170c89-f853-4521-8131-4a0059384154`다. 로컬 `dev`와 운영 사용자에 같다. 코드 블록 7개다.

## 전략 조언자 모드 프롬프트 10가지 Pages 이관 메모

- 원문은 사용자가 채팅에 붙여 넣은 프롬프트 10개다.
- 제목은 `전략 조언자 모드 프롬프트 10가지`다.
- 각 프롬프트는 코드 펜스 하나에 원문 그대로 둔다. `[목표/상황]`, `[아이디어]`, `[시장/고객층]`, `[상품/서비스]`, `[시장/아이디어]`, `[목표]`, `[경쟁자/경쟁 브랜드]`, `[목표/사업/프로젝트]`, `[업무/작업]`, `[목표/전략]`를 윤문하지 않는다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `a36fa357-7c9e-449d-9ecc-e08bb20c9a78`다. 로컬 `dev`와 운영 사용자에 같다. 코드 블록 10개다.

## REBORN 프롬프트 정리기 Pages 이관 메모

- 사용자 URL은 `utm_source=ig&utm_medium=dm&utm_campaign=prompt`가 붙어 있다. 저장 원문은 `https://rebornlabs.kr/prompt`다.
- 문서 제목은 `프롬프트 정리기 — 한 문장을 그대로 쓸 수 있는 프롬프트로`다. OG 제목은 `프롬프트 정리기 · REBORN LABS`다. 페이지 제목은 문서 제목을 쓴다.
- 본문은 랜딩 HTML이다. 이미지는 없다. `funnel-utm.js`와 복사 버튼은 뺀다.
- BEFORE/AFTER `<pre>`와 설치 명령 `reborn-prompt 설치해줘`, `!npx reborn-prompt`, `npx reborn-prompt`는 원문 그대로 코드 펜스에 둔다.
- zip은 `https://rebornlabs.kr/reborn-prompt.zip`로 절대 경로를 쓴다. 클로드킷 링크는 UTM을 빼고 `https://rebornlabs.kr/claudekit`만 남긴다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다.
- 저장 id는 `81453d05-3124-4ee0-9c51-8398d0471a3f`다. 로컬 `dev`와 운영 사용자에 같다. 재실행은 skip이다.

## 네이버 코닥 필름 프롬프트 Pages 이관 메모

- 사용자 URL은 모바일 `PostView`에 Instagram `proxyReferer`와 `fbclid`가 붙어 있다. 저장 원문은 `https://blog.naver.com/ai_newceo/224387395142`다.
- 제목은 `“이거 누가 찍어줬어?” 소리 듣는 코닥 필름 감성 📸`다.
- 본문은 Smart Editor 4 `.se-main-container`다. 프로필, 스티커 GIF, 블로그 아이콘은 뺀다.
- 모바일 `src`는 `mblogthumb` `type=w80_blur`다. 쓰지 않는다. `data-lazy-src`는 `type=w800`이고, PC는 `postfiles` `type=w773`이다. 원본은 호스트를 `blogfiles.pstatic.net`으로 바꾸고 쿼리를 제거한 PNG다. 1장은 약 2.7MB다.
- 영문 코닥 프롬프트는 한 문단이다. 윤문하지 않고 코드 펜스 하나에 둔다.
- 인스타 주소는 `https://www.instagram.com/ai_newpd/`만 남긴다.
- 사용자는 Pages만 요청했다. Prompts 테이블에는 넣지 않는다.
- 같은 제목이고 본문이 같으면 스킵한다. 본문이 다르면 같은 제목 행을 갱신한다. 제목이 달라도 원문 logNo가 본문에 있으면 스킵한다.
- 저장 id는 `c9d48f4a-414b-4635-a504-3c27e806a34e`다. 로컬 `dev`와 운영 사용자에 같다.
- data URL PNG 7장, 코드 블록 1개다. 프롬프트 길이는 1,627자다. `w80_blur`와 `fbclid`는 없다. Prompts 행은 만들지 않았다.
- 운영에 28MB 본문을 한 번에 INSERT하면 statement timeout이 나서, 짧은 stub을 넣은 뒤 같은 행을 UPDATE했다.

## Stars 한글 없는 설명 한국어 추가 메모

- 운영 Star는 336건이다. 한글이 없는 설명은 12건이고 빈 설명은 1건이다.
- GitHub About가 있으면 영문을 유지하고 한국어를 병기한다.
- `revfactory/skills`는 GitHub About가 비어 있어 README를 보고 한국어만 넣는다.
- `freestylefly/awesome-gpt-image-2`는 매핑이 있었지만 본문이 중국어라 한글 검사가 통과하지 않아 백필이 필요했다.
- 운영 반영은 `scripts/add-korean-star-descriptions.mjs`로 한글이 없는 행만 갱신한다.
- 백필 13건(영문 병기 12, 빈 칸 채움 1)을 갱신했고, 한글이 없거나 설명이 비어 있는 항목은 0건이다. 재실행 갱신은 0건이다.

## 찾기 개선 1~8 메모

- 페이지에 노션식 폴더를 두지 않는다. 태그 JSON과 칩 필터로 충분하다.
- 본문 `content.ilike`는 data URL 때문에 느리고 타임아웃이 난다. 검색은 `title`과 `search_text`만 본다. `search_text`에는 `data:image`를 넣지 않는다.
- 원문 URL은 본문 앞부분의 `원문` 링크에서 뽑는다. 별도 필드로 두어 중복 판정과 관련 항목에 쓴다.
- 태그 추론은 출처 호스트와 제목 키워드만 쓴다. 사용자가 고친 태그는 백필이 덮어쓰지 않는다(`tags`가 `[]`일 때만 채움).
- 최근 본은 DB 컬럼을 만들지 않는다. 상세를 열 때 localStorage `mymark:recent-pages`에 id를 쌓는다. 자동저장이 `updated_at`을 오염시키기 때문이다.
- 북마크 카테고리 문자열은 바꾸지 않는다. `PC 북마크/` 접두와 호스트명처럼 보이는 값만 칩에서 그룹으로 접는다.
- 관련 항목은 같은 `source_url` 호스트와 제목 토큰 교집합이다. 임베딩은 쓰지 않는다.
- `⌘K`는 메뉴 필터를 유지하고, 두 글자 이상이면 `/api/search` 결과를 위에 붙인다.
- 운영 `custom_pages` 컬럼은 `supabase/add_page_findability.sql`을 실행한 뒤 `node scripts/backfill-page-findability.mjs`로 채운다. PostgREST로는 ALTER를 못 한다.
- 원문 URL 후보에서 supabase storage·이미지 확장자는 뺀다. 로컬 백필 461건 중 태그는 385건, 원문은 재추출 후 쓰레기 URL 19건을 고쳤다.

## 노션 요리 릴스·워프센스 이미지 프롬프트 20가지 Pages 이관 메모

- 노션 원문은 `https://pineapple-pyroraptor-9c5.notion.site/30-AI-3bf72839155881f38eafd425391cdacf`다. 제목은 `캐릭터로 만드는 30초 AI 요리 릴스 범용 프롬프트`다.
- 이 페이지는 이미 있다. 로컬 `2662a909-e63b-4757-84d3-f87d04ce1b04`, 운영 `5e2f3c63-ed4e-4ee9-9e41-b47cf2927072`. 사용자 요청대로 중복은 스킵하고 본문을 고치지 않는다.
- 워프센스 원문은 `fbclid`가 붙어 있다. 저장 주소는 `https://worpsense.com/chatgpt-이미지-프롬프트-20가지-ai-이미지-스타일-한-번에-바/`의 실제 슬러그(인코딩 포함)다. 제목은 `ChatGPT 이미지 프롬프트 20가지, AI 이미지 스타일 한 번에 바꾸는 방법`이다.
- 본문은 `.elementor-widget-theme-post-content`다. 뉴스레터, 멀린 배너, gravatar, 관련글 썸네일, 도형 이미지는 뺀다.
- 표지와 본문 예시 이미지는 만료되지 않게 data URL로 둔다.
- 20개 스타일 프롬프트는 원문 그대로 Pages 본문에 둔다. Prompts 테이블에는 넣지 않는다.
- 제목 또는 원문 URL이 있으면 수정하지 않고 스킵한다.
- 워프센스 Page id는 `29bd48c5-34a5-4876-bae1-989ba91ae358`다. 본문 이미지 6장, 스타일 키워드 20개다. Prompts 행은 없다. 재실행은 로컬·운영 스킵이다.

## 브랜드 로고 웹사이트 5개 북마크 이관 메모

- 사용자는 로고 생성 사이트 5개를 북마크 `디자인` 카테고리에 넣으라고 했다. Pages·Prompts는 만들지 않는다.
- 주소는 `http://`로 왔지만 저장은 `https`로 올린다. 끝 슬래시는 `bookmarkUrlKey`가 맞춰 비교한다.
- 태그에 카테고리명 `디자인`은 넣지 않는다. 사이트별 태그는 로고·명함·SVG·AI·Shopify·네이밍처럼 역할이 드러나게 붙인다.
- 제목·설명은 사용자가 준 한국어를 쓰고, 표지·파비콘만 `extractMeta`로 채운다.
- 이미 같은 URL이 있으면 스킵하고 카테고리를 바꾸지 않는다.
- 로컬·운영 모두 5건을 신규로 넣었고 재실행은 스킵이다. 북마크 화면의 `디자인` 카테고리는 45건이다.

## 82studio 디자인 참고 사이트 북마크 이관 메모

- 원문은 `https://82studio.notion.site/3446c679d11c80028efaf3733942ef40`다. 페이지 제목은 `[이제만들시간] 바이브코딩 디자인 참고 사이트 모음`이다.
- Notion MCP 워크스페이스에는 없고 공개 `loadPageChunk`·`queryCollection`으로 읽는다.
- 본문 갤러리 컬렉션 `툴 모음`(id `3446c679-d11c-81cb-a895-000b3f4e5ded`, view `3446c679-d11c-8128-a49e-000c799daa7b`)이 사이트 목록이다. space `a3d6c679-d11c-8103-8979-0003ba336b39`.
- 컬렉션 행은 37개다. URL 프로퍼티는 `f:Bt`, 이름 `title`, 한 줄 소개 `jIRe`, 유형 `dN]{`, 세부유형 `PN}D`다.
- URL 속성 값은 `[["https://example.com/",[["a","https://example.com/"]]]]`라서 중첩 문자열을 이어 붙이면 URL이 두 번 붙는다. 첫 문자열만 쓴다.
- Taste skill·ui-ux-pro-max skill·impeccable은 URL이 없어 북마크에 넣지 않는다.
- 사용자 요청은 북마크만이다. Pages·Prompts는 만들지 않는다.
- 카테고리는 북마크 `디자인`이다. 운영에는 이미 이 이름이 있다. 로컬에는 없을 수 있어 없으면 카테고리 마스터에도 넣는다.
- 태그는 세부유형과 유형(디자인 제외)을 쓰고, 세부유형이 비면 한 줄 소개로 2~3개를 붙인다. 카테고리명 `디자인`은 태그에 중복하지 않는다.
- 중복은 `src/lib/bookmark-url.ts`의 `isSameBookmarkUrl`이다. 쿼리와 끝 슬래시만 다른 주소는 같은 것으로 본다. 경로가 다른 주소(예: `21st.dev/`와 `/community/components`)는 별건이다.
- 이미 다른 카테고리에 있는 URL은 스킵하고 카테고리를 바꾸지 않는다.
- 제목·설명은 노션 이름·한 줄 소개를 쓴다. 표지·파비콘은 `extractMeta`로 가져오되 실패하면 비운다.
- URL 있는 행 34개, URL 없는 Skill 3개다. 로컬은 기존 URL이 없어 34건을 넣었고 카테고리 `디자인`을 새로 만들었다. 운영은 기존 URL 12건을 스킵하고 22건을 추가했다. 운영 `디자인` 카테고리는 18건에서 40건이 됐다.
- 재실행은 로컬·운영 모두 신규 0건이다. 북마크 화면에서 `디자인 40` 필터에 유니콘 스튜디오·Pen.dev 등이 보이고 Taste skill은 없다.

## Stars 신규 영문 전용 설명 한국어 병기 메모

- 운영 Star는 323건이다. 한글이 없는 설명은 2026-08-23에 들어온 10건이다. 빈 설명은 0건이다.
- GitHub About가 있으므로 영문을 유지하고 한국어를 병기한다.
- `Leonxlnx/unlazy`는 기존 `Leonxlnx/taste-skill`과 다른 저장소다.
- 백필 10건을 갱신했고, 한글이 없거나 설명이 비어 있는 항목은 0건이다.
